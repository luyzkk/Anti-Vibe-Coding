# State: Contrato Único do Ciclo TDD por Fase

**Plan:** ./PLAN.md
**Phase:** in-progress
**Current Plan:** 02/2
**Last Updated:** 2026-09-08

## Progress por Plano

| Plano | Nome | Fases | Done | Status |
|-------|------|-------|------|--------|
| 01 | Fonte e contrato | 3 | 3/3 | completed |
| 02 | O ciclo roda no execute-plan | 4 | 3/4 | in-progress |

## Progress Global

Fases done: 6/7 (86%)

## Log do Ciclo TDD por Fase

Campos conforme PRD §Observabilidade. O formato definitivo é implementado no Plano 02 fase-03;
aqui já é preenchido à mão para não perder a evidência da fase.

| Fase | red_confirmed | human_gate | red_check | refactor |
|---|---|---|---|---|
| p01/fase-01 | `assertion` (0 pass / 2 fail, ambos `Expected: true / Received: false`) | `skipped(gate ainda não existe — chega no Plano 02 fase-01)` | `pass` ×3 — (1) defesa: linha `**REFACTOR:**` do `fase-template.md` / teste: `fase-template — bloco "### TDD" (RF-02) > o bloco mantem o checkbox REFACTOR`; (2) defesa: heading `## Contrato do Ciclo por Fase` do `tdd-workflow/SKILL.md` / teste: `tdd-workflow — a fonte unica do ciclo (RF-01) > a skill tem a secao ...`; (3) defesa: `(opcional)` na linha 22 de `tdd-cycle-checklist.md` / gate: `grep -n "Passo 7 (opcional)"`. `git diff --stat` vazio após cada restauração. | `none (sem refactor: teste de 2 assertions, helpers copiados do molde write-prd-contract.test.ts)` |
| p01/fase-02 | `assertion` (4 pass / 9 fail; as 9 por `Expected: true / Received: false`. A previsão do doc da fase — "só a skill e REFACTOR passam" — errou: `**RED:**` e `**GREEN:**` já existiam no template. Ver GT-4) | `skipped(gate ainda não existe — chega no Plano 02 fase-01)` | `pass` ×5, cada um derrubando **só** o teste nomeado (`12 pass, 1 fail` nas cinco) — (1) defesa: subitem `- Defesa a mutar:` do checkbox / teste: `o RED-check carrega o campo "Defesa a mutar:" (D6)` — **a string continuou no arquivo dentro do fence do exemplo e o teste caiu assim mesmo: é a prova de que `prose()` funciona**; (2) defesa: linha `- [ ] **RED-check:**` / teste: `o bloco mantem o checkbox **RED-check:**`; (3) defesa: linha `**Tipo de fase:**` / teste: `o bloco declara o tipo da fase com as tres opcoes (D5)`; (4) defesa: comentário HTML do topo do bloco / teste: `o bloco aponta para a secao-fonte na skill tdd-workflow (CA-03)`; (5) defesa: `skills/tdd-workflow/SKILL.md` no §TDD Strategy do README-template / teste: `plan-readme-template — §TDD Strategy aponta para a fonte (D1)`. `git diff --stat` vazio entre as cinco. | `none (sem refactor: motivo real em DI-4 — helpers já têm responsabilidade única; preâmbulo duplicado nas mensagens deixado de propósito para a fase-03 uniformizar)` |
| p01/fase-03 | `assertion` (13 pass / 8 fail; as 8 por assertion, zero regressão nas 13 anteriores) | `skipped(gate ainda não existe — chega no Plano 02 fase-01)` | `pass` ×5, cada um derrubando **só** o teste nomeado (`20 pass, 1 fail` nas cinco) — (1) defesa: palavra `stub` na §TDD do executor / teste: `a secao mantem a regra stub-first`; (2) defesa: bullet "nasce verde" / teste: `a secao mantem a regra nasce-verde`; (3) defesa: bullet do commit `refactor(...)` SEPARADO / teste: `o REFACTOR e commit proprio, separado do feat (D4, CA-08)`; (4) defesa: regra 10 do fence do Step 9 / teste: `as regras proibem prever a mensagem de erro (D6)`; (5) defesa: `"Defesa a mutar"` na regra 9 / teste: `as regras exigem preencher "Defesa a mutar" ...`. `git diff --stat` vazio entre as cinco. | `commit ea6b12f + 0fa6daa` — helper `body()`, rótulos do `test.each`, preâmbulo uniformizado, e rename dos locais que passaram a sombrear `body()`. **Re-provado por 4 mutações re-rodadas** após o refactor (DI-5): refactor de teste pode deixar o gate vácuo sem nada ficar vermelho. |
| p02/fase-01 | `assertion` (21 pass / 6 fail, todas por assertion) | `skipped(gate so existe em runtime apos o sync — fase-04)` | **1a passada: `fail`** — defesa: a linha da regra que classifica `Cannot find module` e afins do passo 2 do 4c / teste: `4c exige que o orquestrador confirme a falha do RED ... (CA-04)` — **nao caiu** (`27 pass / 0 fail` com a regra apagada). Auditoria de multiplicidade achou mais 2 assercoes vacuas (`tdd-cycle-checklist`, `human_gate`); a de `human_gate` confirmada por mutacao. Corrigido em `711d43d`. **2a passada: `pass` ×6**, cada um derrubando so o teste nomeado (`26 pass, 1 fail`): (1) `senao: Assistido (default ...)`; (2) `[--tdd-level ...]` do `argument-hint`; (3) regra de classificacao module-not-found; (4) ponteiro `tdd-cycle-checklist` da mensagem de bloqueio; (5) `Registrar: human_gate: stopped`; (6) frase-fonte do §Ciclo Completo. `git diff --stat` vazio apos cada uma. | `none` — candidato do Passo 7 deixou de existir apos o fortalecimento das assercoes (DI-3) |
| p02/fase-02 | `assertion` (28 pass / 4 fail — **4, nao 5**: o teste D6 nasceu verde porque os dois campos ja estavam no 4c desde a fase-01) | `skipped(gate so existe em runtime apos o sync — fase-04)` | `pass` ×5, cada um derrubando so o teste nomeado (`31 pass, 1 fail` nos cinco). **Auditoria de multiplicidade rodada ANTES de escrever o RED** evitou repetir a fase-01: duas assercoes ja nasceram acopladas. Provas: (1) com a defesa do CA-06 apagada, `git diff --stat` continuava no bloco — um `toContain` teria ficado verde; (5, extra) apagando so `fase blocked;` e mantendo a DI, `blocked` seguia 2x no bloco — de novo um `toContain` ficaria verde. `git diff --stat` vazio apos cada restauracao. | `commit 3094e23` — 17 mensagens de volta a convencao `[parity gate "nunca diminuir" — TAG]` (39/0), que os describes do Plano 02 tinham regredido. Helper `mustContain` recusado com motivo (DI-5). Re-provado por 3 mutacoes pos-refactor. |
| p02/fase-03 | `assertion` (33 pass / 3 fail — tres, nao quatro: `plan-verifier continua read-only (D3)` nasceu verde e foi mutado no mesmo passo, como o `plan-executor.md` §RED manda) | `skipped(gate so existe em runtime apos o sync — fase-04)` | 4 nomeados `pass` (`35 pass, 1 fail` cada). **Mas a regressao completa das 15 defesas das tres fases achou 2 que nao caiam mais**: o GREEN desta fase acrescentou, no mesmo bloco 4c, texto que resgatava as regexes ilimitadas de duas assercoes da fase-02 (CA-06 via a linha `Lista de arquivos tocados`, CA-07 via o formato da linha do STATE log). Consertado em `8462fab` com um limite explicito de alcance na regex (`{0,120}` em vez de ilimitado), com N calculado. Segunda rodada completa: **15/15 caem**. | `commit 4d41fc0` — `step4c` icado para o modulo (era recalculado 3x). Foi a re-prova por mutacao desse refactor que expos as duas assercoes vacuas. |

## Log

- 2026-09-08: Plano criado via /plan-feature (2 planos, 7 fases, ~8.5h) na branch feat/tdd-cycle-contract. Decisões do PRD D1–D6 mapeadas para fases. Tracer bullet = Plano 01 fase-01 (teste de paridade RED → seção-fonte mínima + checkbox REFACTOR). Sequencial entre e dentro dos planos: todas as fases editam o mesmo teste de paridade.
- 2026-09-08: Plano 01 detalhado via /plan-feature (3 fases, 1h cada; README com 6 decisões de planejamento DP-1..DP-6 e gotchas G1–G13). Achados do planejador ao ler os arquivos reais: `skills/lib/__tests__/universal-principles.test.ts` já lê o fase-template (Premissa 3 do PRD corrigida); `docs/references/tdd-cycle-checklist.md` não é rastreado pelo manifest (`docs/` ignorado); as regras do Step 9 do plan-feature vivem dentro de um fence, então a paridade da fase-03 assere com `section()` cru; a fonte cita `--tdd-level` e o 4c desde a fase-01, que só chegam no Plano 02 (DP-4).
- 2026-09-08: Plano 02 detalhado via /plan-feature (4 fases: 1.5h + 1.5h + 1h + 1.5h = 5.5h, igual ao PLAN.md; fase-04 subiu de 1h para 1.5h por ter três rodadas de dogfood: Assistido, negativo `blocked`, `--tdd-level direto`). Primeira tentativa do subagente morreu por limite de sessão sem escrever nada; segunda concluiu. README com DP-1..DP-16 e G14–G23 (G1–G13 herdados). Achados: `plan-verifier.md` §Composition diz que o execute-plan o invoca no Step 5, mas ninguém o spawna — fase-03 corrige para 4c VERIFY (DP-11); `subagent-contract.ts:126` confirma `checks[].name: string` livre, então `red-check-evidence` não toca o parser (CA-10); o script de sync lê o checkout, então o dogfood roda da branch antes do merge; a regex real do guard destrutivo casa `git checkout --`/`git checkout .`/`git restore .`, não `-b` — o bloqueio de 2026-09-08 foi o heredoc do PRD citando `git checkout --` (PLAN.md §Risks corrigido).
- 2026-09-08: **Baseline da branch medida ANTES de qualquer edição**, em `66e6575`: `bun run test` = lote 1 `1450 pass / 0 fail` (175 arquivos) + lote 2 `694 pass / 0 fail` (106 arquivos) = **2144 pass, 0 fail, 281 arquivos**, exit 0; `bun run typecheck` exit 0. O total só aparece somando os dois lotes — o output final mostra apenas o segundo.
- 2026-09-08: **Plano 01 fase-01 (tracer bullet) CONCLUÍDA.** RED em `690cfb1` (`tests/fase-template-tdd-contract.test.ts`, 2 assertions, falha por assertion — confirmado pelo orquestrador rodando o teste, não só pelo subagente). GREEN em `d4993b8`: seção `## Contrato do Ciclo por Fase` em `skills/tdd-workflow/SKILL.md` (linha 547, aditiva, `$ARGUMENTS` segue última linha), checkbox `**REFACTOR:**` no bloco `### TDD` de `fase-template.md`, Passo 7 de `docs/references/tdd-cycle-checklist.md` deixa de ser "(opcional)", manifest regenerado. Os 3 RED-checks por mutação foram executados pelo orquestrador e passaram (ver tabela acima). Suite final `2146 pass, 0 fail` em 282 arquivos (delta = o arquivo novo + suas 2 assertions), typecheck exit 0, harness:validate ok.
- 2026-09-08: Achados registrados no MEMORY do Plano 01 — **BUG-1**: a suite deu 13+1 fail logo após o GREEN por `EBUSY` no fixture `tests/__fixtures__/harness-advanced` (subprocesso `bun` segurando o CWD no Windows sob carga), não por regressão; limpo o resíduo, ficou verde. **GT-1**: `generate:manifest` bumpa `lastModified` de 10 arquivos não tocados (checksum idêntico) — conferir manifest por checksum, não por `git diff --stat`. **GT-2**: `git restore <arquivo>` passa pelo guard, `rm -rf` é bloqueado (mover para lixo). **GT-3**: apagar só o heading derruba o teste com o corpo no lugar — regex ancorada, não `includes`. **DI-3**: o GREEN recebeu os Passos 2-5 da fase (não "apenas os testes") porque o entregável é prosa; sem isso o subagente escreveria um heading vazio que passa o gate.
- 2026-09-08: **Plano 01 fase-02 CONCLUÍDA.** RED em `971e462` (11 assertions novas no teste de paridade + helper `prose()`): `4 pass / 9 fail`, todas por assertion — a previsão escrita no doc da fase errou, e o subagente reportou o literal em vez de ajustar o teste (GT-4). GREEN em `0f83336`: bloco `### TDD` do `fase-template.md` reescrito completo (`Tipo de fase`, 4 checkboxes, `Defesa a mutar`, `Teste que deve cair`, proibição de prever a mensagem, Abuse-It, variante `sem-comportamento`, exemplo preenchido, ponteiro HTML) e `## TDD Strategy` do `plan-readme-template.md` virou ponteiro — a terceira cópia do ciclo morreu. Fidelidade conferida pelo orquestrador por `diff` contra o doc da fase: IDÊNTICO nos dois blocos. Os 5 RED-checks por mutação passaram, cada um derrubando só o teste nomeado. Suite `2157 pass, 0 fail` (delta +11 = as assertions novas), typecheck exit 0, harness:validate ok, manifest com 2 checksums e zero drift.
- 2026-09-08: Achados da fase-02 no MEMORY do Plano 01 — **GT-4**: previsão de pass/fail em doc de fase é chute mesmo quando parece aritmética simples. **GT-5**: o fence do Passo 2 da fase-02 é aninhado (o exemplo preenchido está dentro do bloco ```markdown); extração ingênua trunca — usar intervalos de linha (181-227 e 239-244) e conferir com `grep -n '^```'` antes. **DI-4**: REFACTOR = `sem refactor` com motivo examinado, e uma inconsistência de preâmbulo nas mensagens (`[parity gate "nunca diminuir" — ]` em 5, `[parity gate — ]` em 5) deixada explicitamente para a fase-03 uniformizar.
- 2026-09-08: **Plano 01 fase-03 CONCLUÍDA — Plano 01 fechado (3/3).** RED em `0c89e7b` (`13 pass / 8 fail`, zero regressão). GREEN em `154f9f9`: `agents/plan-executor.md` §TDD deixa de ser a segunda definição do ciclo — aponta para a fonte e incorpora stub-first, "teste que nasce verde exige mutação no mesmo passo", REFACTOR em commit próprio (D4) e o item `fase-{NN}-defesa-implementada` em `payload.checks[]` que o orquestrador vai mutar (D3); `plan-feature` Step 9 ganha as regras 9 e 10 (nomear a defesa; nunca prever a mensagem). `bun run agents:contract` `39 pass` (G7). Os 5 RED-checks passaram. REFACTOR **executado** em dois commits próprios (`ea6b12f`, `0fa6daa`) e re-provado por 4 mutações re-rodadas (DI-5). Suite `2165 pass, 0 fail` (delta +8), typecheck exit 0, harness:validate ok, manifest com 2 checksums e zero drift. Escopo: frontmatter, `## Output Contract`, `## Anti-Degeneration Rules` e `## Formato de Saida` sem uma linha de diff.
- 2026-09-08: Achados da fase-03 — **GT-6**: `test.each` com RegExp no tuple gera testes homônimos (`%s` não interpola), o que inviabiliza nomear "qual teste deve cair"; descoberto **rodando o RED-check**, não lendo o código. **GT-7**: os números de linha escritos nos docs de fase estavam errados nos três casos (fence 51/120 vs 47/121; regra 8 na 759 vs 756; seção do executor termina na 81 vs 82) — conferir com `grep -n` antes de extrair, sempre; um subagente pegou um número errado que o próprio orquestrador repassou. **DI-5**: refactor de arquivo de teste pode deixar o gate vácuo sem nada ficar vermelho — todo `refactor(tests)` fecha com mutação re-rodada.
- 2026-09-08: **Plano 02 fase-01 CONCLUÍDA**, em branch própria `feat/tdd-cycle-contract-plano02` (empilhada sobre `feat/tdd-cycle-contract`, que está na PR #79 — continuar naquela branch faria os commits do Plano 02 caírem na PR do Plano 01). RED em `88947d3` (`21 pass / 6 fail`). GREEN em `2b394b2`: `argument-hint` ganha `--tdd-level`; o `### 4c` vira a sequência 0 RESOLVER NÍVEL → 1 RED → 2 RED CONFIRMADO → 3 GATE HUMANO → 4 GREEN; `wave-execution` §Ciclo Completo aponta para a fonte; +1 linha em Common Rationalizations. Suite `2171 pass, 0 fail` (delta +6), typecheck exit 0, harness:validate ok, preface e2e verde, manifest com 2 checksums e zero drift. Escopo: preface das linhas 10-30 fora de qualquer hunk; blocos JSON de `.tdd-phase.json` do wave-execution intocados.
- 2026-09-08: **O RED-check desta fase FALHOU na primeira passada — e é o achado mais importante da feature até aqui.** O GREEN estava `27 pass / 0 fail`, mas apagar a regra de classificação de module-not-found do 4c **não derrubou** o teste: `toContain('Cannot find module')` estava satisfeito por uma segunda ocorrência, dentro da própria mensagem de erro que a regra emite. Auditoria de multiplicidade de todos os tokens asseridos no bloco achou mais dois vácuos (`tdd-cycle-checklist`, `human_gate`); o de `human_gate` era o pior — aceitava só o ramo `skipped`, então um 4c que nunca parasse para o humano passaria no gate (CA-05 é bilateral). Corrigido em `711d43d`: as três reancoradas no par condição→desfecho, e as mensagens agora explicam por que a regex é acoplada, com a data do vácuo. Segunda passada: 6 RED-checks, todos derrubando só o teste nomeado.
- 2026-09-08: Achados da fase-01 do Plano 02 no MEMORY do plano — **GT-1**: antes de confiar num `toContain`, contar as ocorrências do token na seção alvo; auditar primeiro, mutar depois (mutação sozinha só acha o que você lembrou de mutar). **GT-2**: `diff`/`cmp` bruto sobre arquivo do repo mente — checkout em CRLF (`core.autocrlf=true`); usar `git diff` ou normalizar os dois lados. **GT-3**: terceiro número previsto em doc de fase que não bate (`AskUserQuestion` era 7, virou 8; o checklist previa 3). **DI-1**: `agents/plan-executor.md` §RED já manda "teste que nasce verde exige mutação no mesmo passo" — se essa regra tivesse sido aplicada na escrita do RED, os três vácuos teriam aparecido ali, não no RED-check. O contrato previa; faltou aplicar.
- 2026-09-08: **Plano 02 fase-02 CONCLUÍDA.** RED em `025f40a` (`28 pass / 4 fail` — quatro, não as cinco previstas: o teste D6 nasceu verde porque `Defesa a mutar` e `Teste que deve cair` já estavam no 4c desde a fase-01). GREEN em `915b3ff`: o passo 4 do 4c vira GREEN + REFACTOR (commit `refactor(...)` próprio ou `refactor: none`) e o passo 5 RED-CHECK é escrito — pré-condição, ler os dois campos da fase, mutar, exigir a queda, restaurar com caminho explícito, exigir diff vazio, ramos `pass` / `fail` (fase `blocked` + DI, dependentes não iniciam) / sem-comportamento; o item 1 de `## Regras Críticas` passa a dizer que rodar o teste, mutar e restaurar é **verificação** do orquestrador, não implementação (D3). REFACTOR em `3094e23`. Suite `2176 pass, 0 fail` (delta +5), typecheck exit 0, harness ok, preface e2e verde, manifest com 1 checksum e zero drift. Escopo: zero linha removida dos passos 0–3 do 4c (DP-1 — cada fase só acrescenta).
- 2026-09-08: **A auditoria de multiplicidade rodou ANTES de escrever o RED e evitou repetir a fase-01.** Duas assertions do doc nasceriam vácuas — `toContain('blocked')` (já havia 2 ocorrências via `red_confirmed: blocked`) e `toContain('git diff --stat')` (teria 2 após o GREEN: pré-condição e pós-restore). As duas foram escritas já acopladas ao par condição→desfecho. E foi **provado, não suposto**: no RED-check, com cada defesa apagada, o token continuava no bloco — um `toContain` teria ficado verde nos dois casos. Custo da auditoria: um `grep -c` por token; custo de não tê-la feito na fase-01: um RED-check falho, uma auditoria depois e um commit de conserto.
- 2026-09-08: Achados da fase-02 no MEMORY do Plano 02 — **DI-4** (a auditoria pré-RED e as duas provas), **DI-5** (REFACTOR feito: 17 mensagens tinham regredido a convenção de preâmbulo que o Plano 01 fase-03 estabeleceu; helper `mustContain` recusado com motivo — cobriria 8 de 24 assertions e esconderia as mensagens longas), **GT-3** ampliado (quarta e quinta previsão errada em doc de fase: `## Regras Críticas` está na 842, não 806; o RED teve 4 falhas, não 5) e **GT-4** (`grep -c` conta linhas, não ocorrências — para multiplicidade dentro da mesma linha, `grep -o | wc -l`).
- 2026-09-09: **Plano 02 fase-03 CONCLUÍDA — última fase de código.** RED em `b74dfbf` (`33 pass / 3 fail`; o quarto teste nasceu verde e foi mutado no mesmo passo, como o `plan-executor.md` §RED manda). GREEN em `11a65cf`: o 4c ganha o passo 6 VERIFY (spawn do `plan-verifier` read-only, com RECEBE / NÃO RECEBE / DEVOLVE) e o formato da linha do STATE log; o Step 5 passa a mostrar os quatro campos do ciclo e o custo da fase, com lint condicional e item de bloqueio em `red_check: fail`; o `plan-verifier.md` ganha o item 8 `red-check-evidence` e as duas linhas de exemplo. O §Composition do verifier dizia "etapa Step 5 pos-fase" e **ninguém o spawnava** — uma das mentiras que o PRD apontou; agora diz "Step 4c passo VERIFY" e a causa foi junto. REFACTOR em `4d41fc0`. Suite `2180 pass, 0 fail` (delta +4), typecheck exit 0, `agents:contract` 39 pass, harness ok. Critério humano cumprido: cada campo do STATE log tem um passo que o escreve (`tdd_level` 0, `red_confirmed` 2, `human_gate` 3, `refactor` 4, `red_check` 5, `custo` 6).
- 2026-09-09: **O GREEN desta fase deixou duas assertions da fase-02 vácuas, em silêncio — e só a regressão completa pegou.** As três fases do Plano 02 asserem sobre o mesmo bloco `### 4c.`; o passo 6 e o formato do STATE log introduziram novas ocorrências de `git diff --stat` e de `blocked`, que resgatavam as regexes ilimitadas de CA-06 e CA-07. As duas estavam corretas quando escritas e passaram no RED-check da fase-02. Rodei as **15** defesas das três fases: duas não caíam mais. Consertado em `8462fab` limitando o alcance da regex, com N calculado (não chutado); segunda rodada completa: **15/15 caem**. Falha de processo minha, não do plano: no RED-check da fase-03 eu tinha rodado só as 4 defesas que ela nomeia. Registrado como **DI-6** e **GT-5** (quando várias fases asserem sobre o mesmo bloco, re-rodar TODAS as defesas) e **GT-6** (`[\s\S]*?` sem limite envelhece mal em bloco que cresce).
- 2026-09-09: Um falso vermelho na suite antes da rodada final: `compound-check (skeleton)` com `EPERM` no fixture, 5750ms — mesma família do BUG-1 do Plano 01 (fixtures em `tests/__fixtures__/` no Windows). Passa isolado em 956ms; a suite ficou verde na re-rodada. Não é regressão desta fase.
- 2026-09-09: **PR #80 aberta** (`feat/tdd-cycle-contract-plano02` → `feat/tdd-cycle-contract`, empilhada sobre a #79): 13 commits, 7 arquivos, +565/−94. CI 4/4 verde. Base é a branch do Plano 01, não a `main`, para o diff mostrar só o Plano 02 — merge da #79 primeiro.
- 2026-09-09: **Plano 02 fase-04, Passo 1 (sync do cache) CONCLUÍDO.** Gate textual com ciclo completo: RED — antes do sync o cache tinha `red_check` **0** (e `Contrato do Ciclo por Fase`, `--tdd-level`, `red-check-evidence` também em 0) enquanto o checkout tinha 9; o cache servia mesmo o 4c velho, o que confirma a Premissa 5 na prática. GREEN — `sync-to-global.sh` levou a 9, e `git diff --no-index` ficou vazio em cinco arquivos-chave. RED-check — removidas as linhas com `red_check` do cache, gate caiu a 0; restaurado rodando o sync de novo, voltou a 9 e ficou idêntico ao backup pré-mutação, o que **prova a idempotência** que o cabeçalho do script promete. Isso não prova que o orquestrador executa o ciclo — prova que o texto novo chegou onde o runtime lê.
- 2026-09-09: Fixture do dogfood pronto em `F:\tmp\avc-tdd-dogfood-{template,r1,r2,r3}` com baseline commitado em cada rodada, mais o roteiro em `F:\tmp\avc-tdd-dogfood-ROTEIRO.md`. **DEV-3**: a r2 já foi criada com a mutação inofensiva aplicada, em vez de editar o template entre as rodadas como o doc da fase mandava — tira um passo manual do meio e elimina o risco de esquecer de restaurar. Cada rodada precisa de **sessão nova com cwd no fixture**: a skill entra no contexto no início da sessão, então a sessão que rodou o sync continua com o texto velho.
- 2026-09-09: **r1 fase-01 executada — e o dogfood achou dois defeitos no 4c que nenhuma assertion tinha pegado.** A rodada foi feita pelo orquestrador desta sessão, então é **contaminada para a Premissa 1** (quem rodou escreveu o 4c); vale para o que é mecânico. **Premissa 2 VALIDADA**: a linha com `red_confirmed: assertion` já estava no STATE quando o gate perguntou. **Premissa 4 MEDIDA**: 8 rodadas de teste, 3 spawns; o `plan-verifier` sozinho custou ~25–30% da fase, em cima do limiar do PRD para reconsiderar o RF-05. **CA-10 confirmado em runtime**: o verifier devolveu `red-check-evidence: pass` e o envelope parseou sem tocar no 4d.
- 2026-09-09: **DI-7 — os dois defeitos, corrigidos com RED próprio.** (1) A pré-condição do passo 5 era **inatingível**: o passo 2 grava o STATE antes do gate, o STATE vive no repo do projeto, então `git diff --stat` nunca ficava vazio. (2) A evidência do `red_check` **nunca entrava no histórico** — quem apontou foi o próprio `plan-verifier` da rodada. Os dois tinham a mesma raiz: o 4c escrevia no STATE e nunca mandava commitar. Fix: `git diff --stat -- {arquivo}` escopado ao arquivo da defesa (pré-condição e prova pós-restauração), e commits `docs(state)` nos passos 2 e 6 — o do passo 2 também limpa a árvore para o passo 5. RED em `be474af` (4 assertions, 36 pass/4 fail), GREEN em `8c6cf6e` (40 pass/0 fail). **Regressão completa das 19 defesas: todas caem pelo teste nomeado.** Suite `2184 pass, 0 fail`, typecheck exit 0, harness ok, manifest com 1 checksum.
- 2026-09-09: **O argumento da feature, provado nela mesma.** Os dois defeitos passaram por escrita, revisão, 36 assertions de paridade e três RED-checks por fase — nenhum apareceu. Só apareceram quando o ciclo **rodou** num projeto real. Gate de paridade prova texto; dogfood prova execução. O PRD estava certo em exigir os dois, e a fase-04 deixou de ser formalidade.
- 2026-09-09: Cache re-sincronizado após a correção (`docs(state)` 0→2, `git diff --stat --` 0→2 no cache; `git diff --no-index` vazio contra o checkout). Sem isso, r2 e r3 rodariam contra o 4c sem o fix.
- 2026-09-09: **RF-05 decidido — o `plan-verifier` fica sempre ligado no passo 6 do 4c** (DI-8 no MEMORY do Plano 02). A r1 mediu `custo: testes=8 spawns=3`; a fatia do verifier é 33% em spawns, 25% em rodadas de teste e 29% descontando a dupla-execução do passo 2 — o limiar de 30% do PRD é cruzado ou não conforme a unidade contada. Decidiu o argumento sobre a forma da medida, não a precisão dela: a fase medida é o tracer bullet, o denominador mais baixo possível, e o custo do verifier por fase é quase fixo — logo 25–33% é teto, não valor típico. Somam-se: o verifier achou sozinho o Defeito 2 do DI-7, o nível `direto` já é a válvula de escape, e condicionar por nível exigiria reabrir a fase-03 com RED próprio. **Ressalva:** a Premissa 4 pede 3 fases e temos 1; se r2/r3 mostrarem o verifier acima de 30% numa fase maior que o tracer bullet, a decisão volta à mesa. Quadro "Custo por fase no dogfood" criado no MEMORY (Passo 5 da fase-04) com a r1 preenchida e r2/r3 pendentes.
- 2026-09-09: **r2 e r3 não podem ser rodadas por uma sessão que leu o roteiro.** Retomar a feature exige ler HANDOFF, MEMORY e `F:\tmp\avc-tdd-dogfood-ROTEIRO.md` — e o roteiro lista, rodada por rodada, o desfecho esperado (`red_check: fail` + `blocked` na r2; sem gate mas com verifier na r3). Um orquestrador que já sabe o desfecho não testa se o prompt o produz; seria a contaminação da r1 outra vez, agora com gabarito. As duas rodadas ficam como ação do humano, em sessão nova com cwd no fixture, sem colar o roteiro na sessão.
- 2026-09-09: **r2 e r3 executadas — o mecanismo funciona nos dois casos, e o dogfood achou um TERCEIRO defeito no 4c.** Rodadas feitas por este orquestrador, que leu HANDOFF + MEMORY + roteiro antes de rodar: **contaminadas para a Premissa 1**, e mais que a r1, porque o roteiro lista o desfecho esperado de cada rodada. Valem para o mecânico. **r2 (caso negativo, CA-07 em runtime):** a mutação inofensiva não derrubou `sums two numbers` (exit 0, `1 pass / 0 fail`) → `red_check: fail`, arquivo restaurado sem resíduo, fase `blocked`, DI "teste nao prova a defesa" no MEMORY do fixture, **fase-02 não iniciou**, verifier devolveu `red-check-evidence: fail` com verdict `block`. Premissa 2 revalidada: a linha com `red_confirmed` estava commitada antes de o gate perguntar. **r3 (`--tdd-level direto`):** não parou em nenhuma das duas fases, nem no tracer bullet nem na de risco com bloco `### Seguranca`; RED-check e verifier rodaram nas duas com `red_check: pass`; a mutação da fase de risco (remover `user.id === doc.ownerId`) derrubou o teste de abuso, o que confirma o CA-SEC-1 por mutação.
- 2026-09-09: **DI-9 / GT-7 — o passo 6 do 4c spawna o `plan-verifier` ANTES de commitar a linha do STATE.** O passo 6 manda o verifier RECEBER "a linha do STATE log desta fase", que só fica completa no passo 5, mas a instrução de commit é o **último** bullet do passo 6, depois do spawn. O verifier lê sempre uma linha que existe só na working tree. **Reproduziu 3 vezes em 3 verificações**; nas duas da r3 com `verdict: request_changes` e severidade `high`, e na terceira o próprio verifier classificou como recorrência. É a mesma classe do Defeito 2 do DI-7, um nível acima: o DI-7 fez a evidência chegar ao histórico, não fez chegar antes de quem a lê. **Nenhuma das 40 assertions pega isso** — todas provam que o texto existe, nenhuma prova em que ordem. Correção volta para a fase-03 com RED próprio; não se remenda o 4c dentro do dogfood.
- 2026-09-09: Quatro lacunas menores do 4c registradas na DI-9, todas observadas rodando: o texto não diz se o passo 6 roda quando `red_check: fail`; o passo 6 só dá mensagem de commit para o caminho feliz (`fase-{NN} concluida`) e a r2 fechou `blocked`; `blocked` não existe no vocabulário do STATE que o Step 2 define; e os checkboxes do bloco `### TDD` da fase nunca são marcados por ninguém. Mais duas observações: `human_gate: stopped` é ambíguo para o verifier, que não recebe o 4c e na r2 o leu como "fase parada aguardando humano"; e o `custo` é escrito no passo 6 enquanto o Step 5 roda a suíte depois, então sub-conta sempre.
- 2026-09-09: **Quadro de custo fechado com 4 fases.** Fatia do verifier em rodadas de teste: r1 25%, r2 17%, r3/fase-01 17%, r3/fase-02 22%. Em spawns fica presa em 33% porque toda fase aqui tem exatamente 3 spawns. A queda em rodadas conforme a fase cresce é a forma que a DI-8 previu. **A ressalva da DI-8 continua de pé:** nenhuma das 4 fases medidas é maior que o tracer bullet de forma significativa — a maior tem dois testes.
- 2026-09-09: **Achado do DI-9 CORRIGIDO, com RED próprio na fase-03 — não remendado dentro do dogfood.** RED `6739798` (`40 pass / 3 fail`, as três por assertion, zero regressão nas 40): o passo 5 commita a linha do STATE antes de spawnar o passo 6; o passo 6 declara que a linha já chega commitada; o commit do passo 5 nomeia o caminho `pass` e o caminho `blocked`. GREEN `aece622` (`43 pass / 0 fail`): duas mudanças aditivas, zero linha removida dos passos 0–4 (DP-1). Auditoria GT-1 rodada **antes** do RED — `docs(state)` já aparecia 2x no bloco e `blocked` 4x, então um `toContain` de qualquer um dos dois nasceria vácuo; duas assertions ancoram por linha e a terceira é acoplada com limite `{0,60}` calculado (distância real entre âncoras: 20 caracteres). Os 3 RED-checks nomeados passaram, `42 pass / 1 fail` cada, diff vazio após cada restauração. REFACTOR `none` com motivo. Suite `2187 pass, 0 fail` (delta +3), typecheck exit 0, harness ok, manifest com 1 checksum e zero drift.
- 2026-09-09: **A regressão do GT-5 virou varredura, e o resultado é mais forte que a lista.** Em vez de re-rodar as 19 defesas nomeadas, apaguei uma linha por vez das 118 do bloco `### 4c.`, rodando o teste de paridade a cada deleção com `git restore` entre elas. Das **16** assertions cujo corpo lê `step4c`, **as 16 caem por pelo menos uma deleção** — nenhuma vácua. A lista prova que as defesas que você lembrou de mutar estão guardadas; a varredura prova que nenhuma assertion do bloco sobrevive a toda deleção. Custo: um script de ~15 linhas e ~4 minutos. Registrado como parte do DI-10, com a ressalva de que a primeira leitura do resultado acusou 7 falsos positivos por erro do meu próprio enumerador — GT-3 vale também para número que a minha ferramenta produz.
- 2026-09-09: **Cache do plugin re-sincronizado após o fix do DI-10**, com ciclo de gate textual. RED: antes do sync o cache tinha `ANTES de spawnar o passo 6` **0** e `ja commitada no passo 5` **0**, e `git diff --no-index` contra o checkout acusava 7 deleções — exatamente o GREEN do `aece622` faltando. GREEN: `scripts/sync-to-global.sh` exit 0, os dois marcadores foram a **1** e o `git diff --no-index` ficou vazio nos cinco arquivos-chave. Re-auditoria de multiplicidade no bloco 4c depois do GREEN, registrada na DI-10 para quem editar o bloco a seguir: `docs(state)` passou de 2 para **4**, `blocked` de 4 para **5**, `red_check` **10**. A contagem de ontem não vale amanhã (GT-1).
