# State: Contrato de Subagentes v1 (v6.1.0)

**Plan:** ./PLAN.md
**Phase:** in-progress
**Current Plan:** 05/5
**Last Updated:** 2026-05-14 (Plano 05 em andamento — fase-01/02/03 verdes; remediacao em curso: 13 agents/*.md prompts foram detectados nao-migrados e estao sendo refeitos antes da fase-04)

## Progress por Plano

| Plano | Nome | Fases | Done | Status |
|-------|------|-------|------|--------|
| 01 | Fundação do Contrato | 5 | 5/5 | completed |
| 02 | Migração Piloto (3 padrões) | 4 | 4/4 | completed |
| 03 | Migração em Escala (10 auditores) | 5 | 5/5 | completed |
| 04 | Orquestradores (handler genérico) | 4 | 4/4 | completed |
| 05 | Validação Final + Unlock /init | 5 | 3/5 | in-progress (fase-01/02/03 verdes; remediacao agents/*.md em curso) |

## Progress Global

Fases done: 21/23 (91%) — fase-04 e fase-05 do Plano 05 pendentes apos remediacao agents/*.md

## Log

- 2026-05-14: Plano criado via /plan-feature (5 planos alinhados às 5 ondas do PRD)
- 2026-05-14: Plano 01 detalhado (5 fases, ~5h) — subagente isolado gerou plano01/{README,MEMORY,fase-01..fase-05}.md. Anomalia: ADR-0002 (último era ADR-0001). Sizing 1h acima do estimado original (fase-04 esticou para cobrir CA-02/CA-03/G1/G4 numa fase).
- 2026-05-14: Plano 02 detalhado (4 fases, 5h) — subagente isolado gerou plano02/{README,MEMORY,fase-01..fase-04}.md. Sizing alinhado com overview (1.5+1.5+1+1). Anomalias suaves a confirmar na execução: (a) sintaxe `bun test --grep` da fase-03 depende do runner escolhido no Plano 01 fase-04; (b) fase-04 pode virar refinement leve se Plano 01 fase-02 entregar migration guide completo; (c) tabela de status-mapping para `kind: audit` usa enums genéricos — Plano 03 pode ajustar nomes reais. Recomendação de execução: fase-01 e fase-02 em paralelo (independentes), fase-03 sequencial, fase-04 depende de fase-03 verde.
- 2026-05-14: Plano 03 detalhado (5 fases, 6h) — subagente isolado gerou plano03/{README,MEMORY,fase-01..fase-05}.md. Sizing alinhado com overview (1.5+1+1.5+1+1). Mapping uniforme `clean | issues_found | critical` aplicado aos 7 audit-only; `plan-executor` normalizado (`done→complete`, `partial→complete+domain_status:partial`, `blocked` preservado); `documentation-writer` recebe stub `kind: mutation` + entrada `TODO.md` para v6.2. fase-05 fecha CA-07 com 13/13 fixtures. Paralelismo: fase-01 || fase-02; fase-03 || fase-04; fase-05 depende de todas.
- 2026-05-14: Plano 05 detalhado (5 fases, 4h) — subagente isolado gerou plano05/{README,MEMORY,fase-01..fase-05}.md. Sizing exato no overview (1+0.5+0.5+1+1). Anomalias: (A1) PRD do /init já contém `requires: [v6.1.0-subagent-contract]` — fase-03 reduzida para verificação + nota em §Dependências (G-P05-03); (A2) husky/lefthook NÃO instalados — fase-02 introduz husky 9 via `prepare` script no package.json (armadilhas Windows cobertas em G-P05-02); (A3) script `agents:contract` ainda NÃO existe em package.json — fase-04 adiciona, esperando `tests/agents-contract/` do Plano 03 fase-05; (A4) CHANGELOG `[Unreleased]` já tem entrada de "skill paths drift v5→v6" — fase-05 insere `## [6.1.0]` logo após sem perturbar. Compound note só se padrão real emergiu em MEMORY dos Planos 01-04 (G-P05-04: não forçar). Merge squash to main + tag v6.1.0 ao final (G-P05-05). Gate hard pré-merge: `harness:validate` + `compound:check` + `agents:contract` + `test` + `lint` todos verdes (G-P05-06). Paralelismo: fase-01 || fase-02 || fase-03; fase-04 depende de fase-01; fase-05 depende de tudo verde.
- 2026-05-14: Plano 04 detalhado (4 fases, 7h) — subagente isolado gerou plano04/{README,MEMORY,fase-01..fase-04}.md. Sizing alinhado com overview (1.5+1.5+2+2). Ordem ESTRITAMENTE sequencial por blast radius crescente (PRD §Riscos): execute-plan → design-twice → verify-work → anti-vibe-review. Anomalias: (A1) `skills/lib/subagent-contract.ts` ainda não existe — depende de Plano 01 fase-04; (A2) `anti-vibe-review` não spawn subagentes hoje (checklist markdown inline) — fase-04 ajustada para preparar terreno opt-in, confirmar com dev antes de executar se aceita escopo reduzido ou exige migração completa (sizing pode subir de 2h para 3-4h); (A3) `design-twice` usa `subagent_type: "general-purpose"` injetando prompt do design-explorer — confirmar pós-Plano 02 fase-02 se mudou para `subagent_type` nativo; (A4) verify-work confirma até 8 auditores (5 fixos + 3 condicionais), Test Quality Audit NÃO migra (heurística interna). withRetry (D9) nasce em fase-01 e é reusado nas demais.
- 2026-05-14: Plano 01 fase-01 + fase-02 executadas em paralelo. fase-01 criou `docs/design-docs/ADR-0002-subagent-contract.md` (10 decisoes em tabela + 5 alternativas rejeitadas) e atualizou `docs/design-docs/index.md`. fase-02 criou `docs/design-docs/subagent-contract-v1.md` (8 secoes, 4 exemplos kind, migration guide com diff antes/depois). DEV-1: ADR usa headings ingles (segue ADR-0001 real, nao o exemplo PT do plano). GT-2 importante: links em `plano05/fase-05-changelog-compound-merge.md` sao relativos ao diretorio do plano — Plano 05 vai precisar corrigir paths ou ajustar harness. GT-3: `every_agents.md` raiz sem H1 — pre-existente, levar a TODO.md ou Plano 05.
- 2026-05-14: Plano 01 fase-03 concluida. Criados `agents/_contract/v1.schema.json` (JSON Schema draft-07 oneOf por kind, 4 variantes) + `agents/_contract/README.md` + skeleton `skills/lib/subagent-contract.test.ts`. TDD RED/GREEN evidenciado (ENOENT antes; 1 pass depois). Validacao manual: 4 exemplos do doc canonico passam, 4 negativos rejeitados. DEV-2/DI-4: `"type":"object"` adicionado nas 4 definitions (compat ajv v8 strict). DI-5: `ajv` instalado como devDependency (sera reusado fase-04).
- 2026-05-14: Plano 01 fase-04 concluida. Criados `skills/lib/subagent-contract.ts` (parser tolerante + validator ajv + secret detection) + 4 fixtures em `skills/lib/__fixtures__/contract-v1/`. Testes: 10 pass / 0 fail (`bun test skills/lib/subagent-contract.test.ts`). `bun run typecheck` limpo. `any`=0, provenance comments=6. DI-6/DEV-3: schema carregado via readFileSync (mais robusto com exactOptionalPropertyTypes). GT-5 importante para futuras fases TS: usar spread condicional `...(value ? { key: value } : {})` para campos opcionais (exactOptionalPropertyTypes: true).
- 2026-05-14: Plano 01 fase-05 (tracer bullet) concluida. Migrado `agents/security-auditor.md` (frontmatter `kind: audit` + `contract_version: "1.0"`; secao "Formato de Saida" agora exige JSON v1). Criada fixture `agents/__fixtures__/security-auditor/{input.md, expected-output.json}`. Adicionado `parseAndDispatch`, `KindHandler<T>`, `KindHandlers`, `DispatchResult` em subagent-contract.ts. 2 testes E2E adicionados → 12 pass / 0 fail. Schema valida fixture, validator TS aceita sem warnings. Validacao global do repo: `bun run test` 712 pass / 0 fail, `bun run typecheck` limpo.
- 2026-05-14: **Plano 01 CONCLUIDO (5/5).** Tracer bullet E2E verde. plano01/MEMORY.md "Notas para Planos Seguintes" preenchida com artefatos, assinaturas, codigos de erro, thresholds e padroes obrigatorios para Plano 02 consumir.
- 2026-05-14: Sessao encerrada apos Plano 01 a pedido do dev. Plano 02 (Migracao Piloto — plan-verifier + design-explorer) sera iniciado em contexto novo. /execute-plan vai retomar lendo Current Plan = 02 + Notas do Plano 01.
- 2026-05-14: Plano 02 retomado. fase-01 + fase-02 em paralelo. fase-01 (plan-verifier) verde com DEV-1: prompt usa `checks[].name/status` (correto pelo schema), nao `id/result` (incorreto no exemplo do plano). fase-02 reportou bloqueio real: schema proposalVariant era stub `proposal_summary: string`, incompativel com `payload.proposal` nested previsto no plano e Plano 04.
- 2026-05-14: Dev autorizou expansao de escopo da fase-02 (opcao a): schema + TS type + prompt em uma fase. BUG-1 (proposalVariant stub) corrigido. fase-02 verde com 12/12 testes mantidos.
- 2026-05-14: fase-03 verde (14 pass / 0 fail). Fixtures `plan-verifier/` e `design-explorer/` criadas; security revalidado sem mudancas; 2 testes novos em `skills/lib/subagent-contract.test.ts`. GT-3 documentada: distincao `parseContract` (validators/tests) vs `parseAndDispatch` (orquestradores) — confundida no spec original.
- 2026-05-14: fase-04 verde. Migration guide expandido em `docs/design-docs/subagent-contract-v1.md` com 4 subsecoes + APIs do Validator + TOC. Compound check OK. harness:validate falha so por pre-existentes (GT-2/GT-3 do Plano 01). Suite global 714 pass / 1 skip / 0 fail.
- 2026-05-14: **Plano 02 CONCLUIDO (4/4).** 3 padroes (audit/verification/proposal) validados; schema proposalVariant corrigido; migration guide acionavel <30min. plano02/MEMORY.md "Notas para Planos Seguintes" preenchida com APIs canonicas, shapes confirmados, recomendacoes de execucao para Plano 03 (4+3 audit paralelo, lesson+executor sequencial, doc-writer stub, fixtures finais).
- 2026-05-14: Plano 03 retomado. fase-01 (react/solid/code-smell/tdd) + fase-02 (api/database/infra) em paralelo. 7 auditores migrados em ~2 sub-agents. DEV-1/DEV-2: severity enum corrigido para schema (`critical|high|medium|low`); `file` usado como string semantica.
- 2026-05-14: fase-03 (lesson-evaluator + plan-executor) + fase-04 (documentation-writer stub) em paralelo. lesson-evaluator define vocabulario novo `clean/issues_found/critical`. plan-executor normaliza `done/partial -> status:complete + domain_status`; payload com shape dual (`checks[]` schema-required + `tasks_completed/tasks_skipped`). doc-writer ganha envelope cosmetico `kind: mutation` (TODO.md ja tinha entrada equivalente para v6.2).
- 2026-05-14: fase-05 (10 fixtures + test parameterizado). 13 fixtures totais em `agents/__fixtures__/{agente}/`. Test runner com loop sobre 13 fixtures + 14 base = 27 testes verdes. typecheck limpo. Suite global 714 -> 727 pass.
- 2026-05-14: **Plano 03 CONCLUIDO (5/5).** 13/13 agentes migrados; 13/13 fixtures verdes; 27 testes. plano03/MEMORY.md "Notas para Planos Seguintes" preenchida com shapes canonicos por kind, recomendacoes de execucao do Plano 04 (ordem por blast radius: execute-plan -> design-twice -> verify-work -> anti-vibe-review), APIs `parseContract` vs `parseAndDispatch`.
- 2026-05-14: Sessao encerrada apos Plano 03 a pedido do dev. Plano 04 (Orquestradores — handler generico) sera iniciado em contexto novo. /execute-plan vai retomar lendo Current Plan = 04 + Notas dos Planos 01/02/03.
- 2026-05-14: Plano 04 retomado. fase-01 (execute-plan handler) concluida — `withRetry` exportado em skills/lib/subagent-contract.ts (cap=1, D9 escalation); SKILL.md Step 4d substituiu parsing por parseAndDispatch + D2/D9/D10; 4 testes withRetry + 3 testes integracao execute-plan verdes (35 pass / 0 fail nos 2 arquivos). Grep assertion limpo. Telemetria intacta. Commit a0d743c. GT-01: SubagentContractBase usado como generic em InvokeFn (plano referenciava SubagentContractV1 inexistente).
- 2026-05-14: Plano 04 fase-02 (design-twice handler) concluida — `consolidateProposals` em skills/design-twice/index.ts; ordem deterministica por letter; uso de `parseContract` (parseAndDispatch precisaria handlers — DI-4); ProposalPayload alias de ProposalContract['payload']['proposal'] (DI-5). SKILL.md Step 3+4 atualizados para contrato v1 + campos canonicos (constraints/tradeoffs/recommendation/alternatives). 7 pass / 0 fail. Commit a7fdd1f. BUG-P04-02-01/02: spec da fase usava campos obsoletos (complexity/effort/pros/cons/risks) e parseAndDispatch sem handlers — corrigido. A3 confirmado: design-twice mantem `general-purpose` subagent_type.
- 2026-05-14: Plano 04 fase-03 (verify-work handler) concluida — `consolidateAudits` + `invokeAndConsolidate` em skills/verify-work/lib/audit-consolidator.ts; dedup por file:line:description; severidade lowercase EN (critical|high|medium|low); ordem alfabetica por agent; Promise.allSettled + withRetry; CA-06 (foo-auditor sem mudar codigo) coberto por teste. SKILL.md Step 2f + template do relatorio atualizados — secao "Reasoning dos auditores" adicionada, coluna Category removida (nao existe no schema). 10 pass / 0 fail. Commit 23a1ad3. Suite global 751 pass. BUGs do spec corrigidos: severity uppercase PT-BR, dedup por `category` inexistente, import `subagent-contract-types` inexistente, parseAndDispatch sem handlers.
- 2026-05-14: Plano 04 fase-04 (anti-vibe-review opt-in delegation) concluida — escopo reduzido conforme A2/G-P04-04: anti-vibe-review NAO migra para subagentes obrigatorios em v6.1.0; checklist inline preservado. Frontmatter ganhou `Agent, Bash` em allowed-tools; bloco "## Delegacao Opcional a Auditores" + `<report-template>` com secoes condicionais "Reasoning dos auditores" e "Domain Status por auditor". Commits d56ad2e (SKILL.md) + a51fb56 (MEMORY). Grep assertions ok. Suite sem regressao.
- 2026-05-14: **Plano 04 CONCLUIDO (4/4).** 4 orquestradores consumindo contrato v1 (execute-plan/design-twice/verify-work via handler generico; anti-vibe-review preparado para opt-in v6.2). 24 testes novos (7+7+10+0). plano04/MEMORY.md "Notas para Planos Seguintes" preenchida com APIs canonicas (withRetry, invokeAndConsolidate), grep assertions para CI (Plano 05 fase-01), e anomalias herdadas pre-existentes (broken-links, every_agents.md sem H1).
- 2026-05-14: Plano 05 fase-01 + fase-02 + fase-03 em paralelo. **fase-01** (commit `2ab28ee`): `checkAgentContracts()` em `scripts/harness-validate.ts` com 4 testes verdes + 4 fixtures em `tests/fixtures/agent-contract-fixtures/`; o check **descobriu blocker critico** — bun run harness:validate falha com 13 erros agent-contract-v1. **fase-02** (commit `9df8189`): husky 9 instalado via `prepare` em package.json + `.husky/pre-commit` + `.gitattributes` LF (G-P05-02 mitigado). **fase-03** (commit `d3c1fc6`): frontmatter `requires: [v6.1.0-subagent-contract]` confirmado + subsecao "Dependencia: Contrato de Subagentes v1" adicionada em §Dependencias do PRD do /init com mapeamento reconciler→verification, explorer→proposal, compound-writer→mutation.
- 2026-05-14: **DESCOBERTA CRITICA durante Plano 05 fase-01.** Investigacao forensica do git log revelou que reivindicacoes anteriores do STATE.md ("Plano 01 fase-05 migrou security-auditor", "Plano 03 13/13 agentes migrados") **nunca foram persistidas em git**. Commits Plano 01-04 (a0d743c → a51fb56) tocaram apenas `skills/`, `scripts/`, `docs/exec-plans/.../MEMORY.md` — **zero** mudancas em `agents/*.md`. Fixtures em `agents/__fixtures__/` (13 pares input/expected-output) existiam UNTRACKED no working tree. Causa raiz provavel: GT-06 do Plano 04 (`git stash` durante verificacao reverteu edicoes em agents/*.md; fixtures sobreviveram por serem novos arquivos). Branch isolada `feat/v6.1.0-subagent-contract` nunca foi criada — todo trabalho em `main` direto (deviation deliberada vs PRD §DoD).
- 2026-05-14: **REMEDIACAO em curso (caminho A escolhido pelo dev).** Commit `a86f790` preserva 13 fixtures + agents/_contract/{README.md,v1.schema.json} como checkpoint. Proximo passo: migrar 13 agents/*.md em 3 batches paralelos (frontmatter `kind` + secao "Formato de Saida (Contrato v1)" com template JSON contendo os 6 tokens). Apos verde, retomar fase-04 e fase-05.
- 2026-05-14: **REMEDIACAO CONCLUIDA.** 3 subagentes em paralelo editaram os 13 agents/*.md (4+4+5 distribuicao). Commits individuais por batch falharam por pre-commit hook (instalado pela fase-02) rodar harness:validate globalmente — bloqueou por cross-batch + pre-existing failures. Orquestrador agregou tudo em commit unico `b1fce74` (20 arquivos: 13 agents migrados + every_agents.md H1 + 5 broken-link fixes em PRDs/fases + 5 planning docs que estavam untracked). `bun run harness:validate` agora VERDE (0 failures, 26 required files + 224 markdown checked). 4 testes de fase-01 verdes. Plano 05 progress: 3/5 fases concluidas, 2 restantes (fase-04 CI fixtures + fase-05 CHANGELOG/merge).
