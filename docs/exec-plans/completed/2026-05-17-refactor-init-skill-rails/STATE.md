# State: Refatoracao Rails-style do SKILL.md do /anti-vibe-coding:init

**Plan:** ./PLAN.md
**Phase:** completed
**Current Plan:** 04/4
**Last Updated:** 2026-05-17 (feature CONCLUIDA — 5/5 plano 04)

## Progress por Plano

| Plano | Nome | Fases | Done | Status |
|-------|------|-------|------|--------|
| 01 | Foundation + Tracer Bullet | 4 | 4/4 | completed |
| 02 | Steps puros (nao-interativos, sem gates) | 6 | 6/6 | completed |
| 03 | Gates de abortagem + steps interativos | 6 | 6/6 | completed |
| 04 | Extracao de rationale + Akita + Cutover | 5 | 5/5 | completed |

## Progress Global

Fases done: 21/21 (100%)

## Log

- 2026-05-17: Plano criado via /plan-feature
- 2026-05-17: Plano 02 detalhado via /plan-feature — 6 fases criadas em plano02/, MEMORY.md inicializada. Contrato `StepReport.skipRemaining?: boolean` introduzido na fase-06 (extensao aditiva ao contrato de Plano 01 fase-01).
- 2026-05-17: Plano 03 detalhado via /plan-feature — 6 fases criadas em plano03/, MEMORY.md inicializada. Extensoes aditivas ao contrato (fase-06): `StepReport.needsUser?: { prompt, options }` + `StepContext.askUser?: (prompt, options) => Promise<string>`. 3 DIs flagged para revisao do dev: DI-1 (fase-02, guard interno de `isMigrateMode` em vez de `shouldRun` no contrato), DI-3-1 (fase-03, reason da AbortError de migrate.2 conflict carrega 6 linhas completas), DI-5-1 (fase-05, migrate-all roda APENAS em dry-run — real mode delega para migrate.1/2/3/4 individuais).
- 2026-05-17: Plano 04 detalhado via /plan-feature — 5 fases criadas em plano04/ (extracao rationale + Akita + cutover SKILL.md + E2E byte-idempotence + final validation), MEMORY.md inicializada. 4 DIs flagged para revisao do dev: DI-3-1 (fase-03, golden pre-cutover capturado via dispatcher dos Planos 02/03 antes do cutover), DI-3-2 (fase-03, steps table em markdown — NAO YAML frontmatter — para evitar conflito com loader Claude Code), DI-3-3 (fase-03, condicional: telemetry pode escalar se exceder 200 linhas), DI-4-1 (fase-04, divergencia golden pre/pos-cutover = sintoma de cutover quebrado, NAO atualizar golden). Gotchas escalados: (1) `bun run lint` nao existe no package.json — substituido por `bun run typecheck` nas fases; (2) cross-reference grep gate (fase-05) depende de convencao de ID consistente decidida em fase-01; (3) `assets/snippets/akita-*.md` segue convencao de `delivery-loop.md`.
- 2026-05-17: Plano 01 fase-01 concluida — commit 59edb88. Criados skills/init/lib/steps/{types.ts, abort-error.ts, types.test.ts, abort-error.test.ts}. 1169 tests pass / 0 fail (5 novos incluidos). Confirmado: `bun run lint` nao existe (so typecheck via tsc --noEmit). GT-1 capturado em MEMORY: `import type` apagado em runtime — types.test.ts nao falha em RED por modulo inexistente, comportamento esperado.
- 2026-05-17: Plano 01 fase-02 concluida — commit 405d9c5. Criados skills/init/lib/{parse-flags.ts, registry.ts, run-init.ts, run-init.test.ts, parse-flags.test.ts}. 7 testes novos (4 dispatcher + 3 parser), todos passando. Pattern DI-06 (await import) ja aplicado no dispatcher como antecipacao da fase-04. Tipos de fase-01 funcionaram sem ampliacao — G3 (contrato Step estavel) confirmado.
- 2026-05-17: Plano 01 fase-03 concluida — commit 42f94d5 (TRACER BULLET ✓). Criados skills/init/lib/steps/00-detect-legacy.ts (wrapper de detectV5Legacy) + 3 fixtures (greenfield/legacy/partial) + 3 goldens + 00-detect-legacy.test.ts. Registry agora tem detectLegacyStep como primeira entrada. SKILL.md e helper detect-v5-legacy.ts intocados. Byte-identidade confirmada via golden tests + paranoia grep das 5 strings literais. Dispatcher end-to-end OK em greenfield fixture: { kind: 'ok', report: { mutated: false, summary: 'all steps completed' } }. Gotcha: .planning/ esta no .gitignore — fixtures legacy precisaram git add -f (intencional, simula projeto legado).
- 2026-05-17: Plano 01 fase-04 concluida — commit da416c5. Criado helper `skills/init/lib/lazy-import.ts` que centraliza pattern DI-06/GT-04. run-init.ts refatorado para usar `lazyImport(() => import('./registry'))`. Teste de cwd Windows-style adicionado como guardrail. 16 testes pass / 0 fail. harness:validate green (26 required files, 201 markdown checked).
- 2026-05-17: ✅ **PLANO 01 COMPLETO** (4/4 fases). Infraestrutura Rails-style entregue: tipos + AbortError + dispatcher + tracer step + helper DI-06. SKILL.md continua intocado (cutover sera no Plano 04). Planos 02 e 03 podem iniciar em paralelo — interface Step e lazyImport sao estaveis.
- 2026-05-17: Plano 02 fase-01 concluida — commit 2188502. Step 1 portado em `skills/init/lib/steps/01-scaffold-full-tree.ts`, registry agora tem 2 entradas. 3 testes novos pass. Regressao do init: 395 pass / 0 fail. GT-P02F01-01 capturado: `scaffoldFullTree` JA cria TODO.md via template manifest — golden ajustado para 'ja existe' em ambos cenarios (greenfield e with-todo).
- 2026-05-17: Plano 02 fase-02 concluida — commit 26b8b10. Step 2 portado em `skills/init/lib/steps/02-link-claude-agents.ts` com DI via `runLinkClaudeStep(targetDir, linker?)` (evita mock.module pollution). Registry com 3 entradas. 3 testes pass (symlink/hardlink/copy-with-hook — CA-08). 398 testes pass / 0 fail. DEV-01: stub de `LinkResult` no plano usava `source`/`target` (campos inexistentes); shape real e `{ tier, targetPath, hookRegistered }` — ajustado antes de escrever testes.
- 2026-05-17: Plano 02 fase-03 concluida — commit 6cfa826. Step 3 e Step 3.1 portados (`03-detect-stack-and-register.ts`, `03_1-persist-stack-and-knowledge.ts`). Registry com 5 entradas. 5 testes novos pass (3 detect-stack + 2 persist com runner stub). 403 testes total. DEV-2: regex do plano `[a-z-]+` para signalSource nao bate com valores reais (`package.json#dependencies.next`) — ajustado para `\S+` / `.+`. DEV-3: stub de `RunStackKnowledgeInitResult` no plano usava `{ stackPersisted, knowledgeCopied }` — shape real e `{ stackPrimary, stackJsonMessage, copyResult, previewEmitted }`. DEV-4: golden de `unknown` mudou `fallback` para `no signal` (string real). G5 preservado: parseRefreshFlag aparece so em comentario.
- 2026-05-17: Plano 02 fase-04 concluida — commit e418779. Step 4 portado em `04-customize-architecture.ts` (envelopa customize + re-detecta stack — preserva byte-identicality). Registry com 6 entradas. 2 testes novos. 405 testes total. DEV-5: marker real do helper e `<!-- INIT:STACK_BLOCK -->` (nao `INIT:DETECTED_STACK_SLOT` como o plano sugeria). DEV-6: helper NAO eh tolerante a ARCHITECTURE.md ausente (lanca ENOENT) — segundo teste ajustado para `expect throw` (G2: nao patchar helper).
- 2026-05-17: Plano 02 fase-05 concluida — commit d313cbf. Step 5 portado em `05-install-gh-files.ts` (D14: sempre roda, sem `shouldRun`). Registry com 7 entradas. 2 testes (greenfield + idempotente). 407 testes total. Confirmado: `result.filesWritten` e `ReadonlyArray<string>` — interpolacao `${...}` chama `.toString()` e produz comma-joined list (byte-identico ao SKILL.md).
- 2026-05-17: Plano 02 fase-06 concluida — commit 832c369. Steps `00_1-reuse-discovery.ts` + `90-final-validation.ts` portados. `StepReport` estendido com `skipRemaining?: boolean` (aditivo). Dispatcher patcheado para honrar `skipRemaining`. Registry com 9 entradas (ordem-alvo do PRD). 6 testes novos. 413 testes total. UMA excecao a G3 documentada: `await import` dentro do callback de `tryRegenerateParityGaps` (assinatura do helper).
- 2026-05-17: ✅ **PLANO 02 COMPLETO** (6/6 fases). 9 steps puros portados, registry final tem ordem canonica do SKILL.md. SKILL.md continua intocado. `StepReport` extension aditiva (`skipRemaining`) — dispatcher honra early-exit. Plano 03 (gates + interativos) pode iniciar — interface estavel, ordem do registry conhecida.
- 2026-05-17: Plano 03 fase-01 concluida — commit dd3c007. Criado `skills/init/lib/run-init-abort.test.ts` com 4 testes regressao (code=1 needs-migration, code=2 ambiguity, code=1 migrate.2 conflict 6 linhas, downstream interrompido) + 3 goldens em `__golden__/abort-*.txt`. Dispatcher do Plano 01 fase-02 ja captura AbortError corretamente — todos os 4 testes passaram imediatamente, sem patch necessario. Paranoia grep 6/6 strings presentes no SKILL.md. Suite total: 1208 pass / 1 skip / 0 fail. SKILL.md e run-init.ts intocados.
- 2026-05-17: Plano 03 fase-02 concluida — commit c85b2f7. Step migrate.1 portado em `skills/init/lib/steps/10-migrate-1-backup.ts` (envelopa `backupPlanning`, AbortError code=1 em lock/permissao). Registry com 10 entradas (migrate1BackupStep inserido em indice 2 — posicao provisoria ate fase-05 reordenar para colocar migrate0/migrateAll antes). 5 testes novos (no-op/created/idempotent/lock-abort/dry-run). 1213 pass / 0 fail. Helpers preservados. DI-1: isMigrateMode interno via args[0]==='migrate' (consistente com reuseDiscoveryStep, contrato minimo). GT-1: spec de testes inclui dry-run alem dos 4 cenarios da tabela — implementados todos 5.
- 2026-05-17: Plano 03 fase-03 concluida — commit 9dc807d. Step migrate.2 portado em `skills/init/lib/steps/11-migrate-2-planning.ts` (envelopa `migratePlanning`, AbortError code=1 com reason de 6 linhas em conflict, byte-identico ao SKILL.md linhas 143-150). Registry com 11 entradas (migrate2PlanningStep apos migrate1BackupStep). 4 testes (no-op/ok-no-conflicts/conflicts-throws-abort/dry-run). 1217 pass / 0 fail. DEV: fixture do plano sugeria 'exec-plans/foo.md' mas parsePlanningEntry classifica como 'unknown' — corrigido para '2026-05-12-foo/PLAN.md' (plan-folder-plan kind).
- 2026-05-17: Plano 03 fase-04 concluida — commit b585b12. Steps `12-migrate-3-lessons.ts` + `13-migrate-4-decisions.ts` portados (best-effort, sem AbortError). Registry com 13 entradas. 6 testes novos (3+3 cenarios). 1223 pass / 0 fail. DEV-1: fixture do spec dizia `.planning.v5-backup/.planning/lessons-learned.md` mas helper `migrateLessons` usa `BACKUP_DIR/lessons-learned.md` direto — fixture ajustada (G2). DEV-2: SKILL.md linha 174 usa `s.reason.includes('source-missing')` mas helper retorna `reason='no lessons-learned.md in backup'` (sem essa substring). Step usa `report.status === 'skipped'` como predicado equivalente — divergencia EXISTE entre SKILL.md atual e helper real. **Plano 04 fase-03 (cutover) DEVE alinhar SKILL.md com o predicado real do helper** (ou ajustar o helper para retornar reason com 'source-missing'). Atualmente o STEP eh consistente com o helper, nao com o SKILL.md.
- 2026-05-17: Plano 03 fase-05 concluida — commit 81edd14. Steps `09-migrate-0-parse-dry-run.ts` + `09_1-migrate-all-orchestrate.ts` portados. Registry com 15 entradas (reordenado para ordem-alvo: migrate0/migrateAll antes de migrate1..4). 7 testes novos (3+4). DI-5-1 aplicada: migrate-all em dry-run roda + skipRemaining=true; em real mode no-op. **Nota subtil:** `'Re-run without --dry-run to apply.'` aparece DUAS vezes no output total — uma do renderer, outra concatenada ao summary do step. Funcionalmente correto (endsWith ainda bate), mas Plano 04 fase-04 (E2E byte-idempotence) deve confirmar se SKILL.md atual tambem repete a frase ou se ha drift.
- 2026-05-17: Plano 03 fase-06 concluida — commit b974680. Steps `14-delivery-loop.ts` (Step 6) + `15-capabilities-discovery.ts` (Step 7) portados. Contrato estendido: `StepReport.needsUser?: { prompt, options }` + `StepContext.askUser?: (prompt, options) => Promise<string>`. Dispatcher (`run-init.ts`) com 3a mudanca acumulada: pause+ask+resume + anti-loop guard. Registry com 17 entradas — ORDEM-ALVO FINAL atingida. 9 testes novos (4 delivery-loop + 3 capabilities + 2 dispatcher needsUser). 435 testes em skills/init/lib/. Suite total: 1239 pass / 1 skip / 0 fail. G6 (mutual exclusion needsUser/skipRemaining) e G7 (Step 7 nunca lanca) provados em teste. exactOptionalPropertyTypes: askUser injetado via guard condicional para evitar `undefined` no spread.
- 2026-05-17: ✅ **PLANO 03 COMPLETO** (6/6 fases). 8 steps adicionados (migrate.0/all/1/2/3/4 + delivery loop + capabilities). Contrato estendido com `needsUser` + `askUser` (aditivos a `skipRemaining` de Plano 02). Dispatcher acumula 3 capacidades: catch AbortError + skipRemaining + needsUser pause-and-resume. **Registry com ordem-alvo final de 17 entradas — pronto para cutover do SKILL.md em Plano 04 fase-03**. Suite verde: 1239 pass / 0 fail. 2 DEVs criticos flagged para Plano 04: (1) divergencia `s.reason.includes('source-missing')` em SKILL.md vs reason real do helper migrateLessons; (2) possivel double-emission de `'Re-run without --dry-run to apply.'` em dry-run output.
- 2026-05-17: Plano 04 fase-01 concluida — commit e92c3cf. Criado `docs/design-docs/init-rationale.md` com 34 entradas `###` (>= 15 esperado), 35 `Consumido por:` (>=15 esperado). Index `docs/design-docs/index.md` atualizado com secao "Init Rationale". Divergencia DEV-P04F01-1 documentada na secao DEV historic do proprio arquivo (SKILL.md linha 174 vs migrate-lessons helper — confirma o DEV-P03F04-2). IDs extras descobertos alem do checklist minimo: CA-01/03/04/05, D1/D2/D4/D16. DI-4 (sem hifen) preservado como alias para nao quebrar grep. SKILL.md e lib/ intocados (`git diff` vazio). harness:validate OK (202 markdown files). 1240 pass / 1 skip / 0 fail.
- 2026-05-17: Plano 04 fase-02 concluida — commit e4690be. Criados 5 snippets em `skills/init/assets/snippets/akita-*.md` (code-style / comments / tests / dependencies / logging) com extracao byte-identica das linhas 1015-1193 do SKILL.md modulo o downgrade `### Secao: X` -> `## X`. Paranoia diff de frases-assinatura (Nomes grepáveis, Escreva o WHY, F.I.R.S.T, Injeção de dependência, JSON estruturado) confirmou presenca em ambos. GT-P04F02-1: git emitiu warnings LF->CRLF na staging (Windows `core.autocrlf=true`); armazenamento interno mantem LF (esperado, nao bloqueia). SKILL.md e lib/ intocados. 1239 pass / 0 fail / 186 files. **Pronto para fase-03 (cutover).**
- 2026-05-17: Plano 04 fase-03 concluida — commit f372117. SKILL.md reescrito como manifest declarativo: 1215 linhas -> 86 linhas (CA-09 <=200). Bloco fenced unico chama `runInit` via dispatcher. Tabela de 17 steps com IDs, slugs e status. Referencias a rationale + snippets Akita. Removidos blocos inline (>1100 linhas). Helpers preservados. bun test verde (suite total 1239 pass). harness:validate + compound:check OK.
- 2026-05-17: Plano 04 fase-04 concluida — commit e47ca88. 8 testes E2E (4 greenfield + 4 legacy-v5) com 4 goldens em `tests/e2e/__golden__/`. Byte-idempotence confirmada: stdout e tree.json identicos pre/pos-cutover. Cenarios cobertos: greenfield, legacy-abort, dry-run, capabilities-soft-fail, backup-abort, planning-conflict (CA-01, CA-02, CA-03, CA-06, CA-07). CA-07 em Windows necessitou workaround com orchestrateMigration direto (chmod nao funciona no Windows — GT-P04F04-1). harness:validate + compound:check OK.
- 2026-05-17: Plano 04 fase-05 concluida (FEATURE CONCLUIDA). Bateria final verde. 19 IDs transversal no cross-reference gate (DI-P04F05-1 documentado). MEMORY.md de planos 01/02/03/04 atualizados com nota de cutover. STATE.md: feature refactor-init-skill-rails = completed, 21/21 fases, 100%. Rollback seguro: `git revert f372117` restaura SKILL.md inline. Licoes para /lessons-learned: padrao Rails-style, cutover big-bang com goldens, cross-reference grep gate.
