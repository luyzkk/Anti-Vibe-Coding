# State: Anti-Vibe Coding v6.0.0 — Harness + Compound Fusion

**Plan:** ./PLAN.md
**Phase:** completed
**Current Plan:** 09/9
**Last Updated:** 2026-05-12 (**v6.0.0 GA LOCAL** — Plano 09 5/5 done. Tag anotado `v6.0.0` criado em `4c18844`. Rollback test PASS. Push para origin pendente de autorizacao manual do dev.)

## Progress por Plano

| Plano | Nome | Fases | Done | Status |
|-------|------|-------|------|--------|
| 01 | Tracer Bullet — Minimal `/init` + Validator E2E | 5 | 5/5 | **completed** |
| 02 | Full Scaffold | 6 | 6/6 | **completed** |
| 03 | Migration v5→v6 | 7 | 7/7 | **completed** |
| 04 | Validators Full | 5 | 5/5 | **completed** |
| 05 | Skill Migration + Hooks | 8 | 8/8 | **completed** |
| 06 | Agent-Native (D31/D32/D33) | 7 | 7/7 | **completed** |
| 07 | TODO.md + /todo-pick | 4 | 4/4 | **completed** |
| 08 | Dog-Fooding (POR ÚLTIMO) | 8 | 8/8 | **completed** |
| 09 | Versioning & Release | 5 | 5/5 | **completed** |

## Progress Global

Fases done: 55/55 (100% — **TODOS OS 9 PLANOS COMPLETOS**)
Sizing total: ~87h (~4 semanas focadas)

## Log

- 2026-05-11: Plano criado via /plan-feature (9 planos, 55 fases). Aprovação aprovada pelo dev.
- 2026-05-11: Plano 01 detalhado gerado por subagente isolado. 7 arquivos criados em plano01/ (README, MEMORY, fase-01 a fase-05). Sizing total ~7h. 5 ambiguidades sinalizadas pelo subagente (G1-G5 documentados em README). Próximo: ou criar Plano 02 detalhado, ou começar execução via /execute-plan.
- 2026-05-11: Plano 02 detalhado gerado por subagente isolado. 8 arquivos em plano02/ (README, MEMORY, fase-01 a fase-06). Sizing total ~10h. Ordem de execução recomendada documentada no README: 01→02→06→(04‖05)→03 (fase-03 consome DetectedStack de fase-06). 3 ambiguidades sinalizadas: G-A1 tech-debt-tracker.md, G-A2 placeholders FRONTEND/db-schema, G-A3 copy do Delivery Loop. Próximo: criar Plano 03, ou iniciar execução via /execute-plan.
- 2026-05-11: Plano 03 detalhado gerado por subagente isolado (API 500 no relatório final, mas todos 9 arquivos foram escritos; trailing `</content>` / `</invoke>` removidos manualmente). 9 arquivos em plano03/ (README, MEMORY, fase-01 a fase-07). Sizing total ~12.5h. DAG: 01 → 02 → (03‖04‖05) → 06 → 07. Decisão de design: dry-run (fase-06) é cross-cutting depois das mutações já existirem, não antes — refactor mecânico em vez de virtual FS prematuro. 4 ambiguidades sinalizadas: G-A1 deletar `.planning/` original após migração, G-A2 destino de STATE.md v5 legado, G-A3 senior-principles.md fora do contexto dog-food, G-A4 formato do diff de dry-run. Próximo: criar Plano 04, ou iniciar execução via /execute-plan.
- 2026-05-11: Plano 04 detalhado gerado por subagente isolado. 7 arquivos em plano04/ (README, MEMORY, fase-01 a fase-05). Sizing total ~7h. Ordem recomendada: serial 01→02→03→04→05; paralelizável em worktrees (01→02 ‖ 03; depois 04 ‖ 05). 4 ambiguidades sinalizadas: 04-A1 match estrito de H2 Problem/Solution/Prevention vs aliases, 04-A2 tags >=1 hard erro vs warn, 04-A3 `<2s` per-validator vs soma agregada, 04-A4 bench mede mediana de 3 runs após warmup. Descobertas: required-files cresce de 22 (Andre) → 25 em v6.0.0; lib/compound-frontmatter.ts duplica lógica no .tpl (sync manual via JSDoc); G5 Windows Defender warmup + G10 `_archived/` exclusion necessários para CA-26/CA-28 estáveis. Próximo: criar Plano 05, ou iniciar execução via /execute-plan.
- 2026-05-11: Plano 05 detalhado gerado por subagente isolado. 10 arquivos em plano05/ (README, MEMORY, fase-01 a fase-08). Sizing total ~13.5h. Ordem recomendada: serial 01→02→03→(04‖05)→06→08; fase-07 (hook) paralelizável desde o início (~8h wall-clock com 2 worktrees). 6 ambiguidades sinalizadas: 05-A1 escopo das 6 skills migradas (grill-me/write-prd permanecem em .planning/), 05-A2 lessons-learned em projeto v5 puro (silent tip vs prompt interativo), 05-A3 CA-17 = 18 chamadas (3 sintaxes × 6 skills), 05-A4 status `pending-capture` no frontmatter para Compound Gate "pensar mais", 05-A5 7 seções do /quick-plan, 05-A6 hook discoverability via hooks.json + CHANGELOG. Descobertas cross-plan: D26 negative-list precisa short-circuit antes de verbos positivos; `pending-capture` afeta validador Plano 04 + STATE.md Plano 06; hook lock compartilhado com user-prompt-gate.cjs (TTL 5s em ~/.claude/cache/) mitiga R6; assinatura union `(string | Opts)` formaliza D10 em todas as 6 skills; projeto cru (sem v5/v6) trata como v5 default para preservar D10. Próximo: criar Plano 06, ou iniciar execução via /execute-plan.
- 2026-05-11: Plano 07 detalhado gerado por subagente isolado. 6 arquivos em plano07/ (README, MEMORY, fase-01 a fase-04). Sizing apurado 5.5h (+30min vs 5h estimado — fase-04 mais robusta). Tracer Bullet: fase-03 (skill /todo-pick funcionando). Ordem recomendada: paralelo (fase-01 ‖ fase-02 ‖ fase-04) → serial fase-03 (~3h wall-clock). 8 ambiguidades sinalizadas: 07-A1 fase-02 estende todo-utils com pickNext/listPending/filterByStatus/scoreByPriority/parseLine (preserva sizing), 07-A2 encoding UTF-8 sem BOM/LF, 07-A3 race em addLine aceita, 07-A4 Scope D18 assumido como array glob (dep crítica de Plano 05 fase-03), 07-A5 file: vs feature:, 07-A6 índices 1-based skill / 0-based helper, 07-A7 --add fora de v6.0.0, 07-A8 pickNext só 'oldest'. Cross-cutting: dep oculta Plano 05 fase-03 (Scope parseável); todo-utils pode crescer >300 linhas (sugestão split em todo-utils + todo-pick-utils); CA-44 parcialmente já entregue por Plano 06 fase-07. Próximo: criar Plano 08, ou iniciar execução via /execute-plan.
- 2026-05-11: Plano 09 detalhado gerado por subagente isolado. 7 arquivos em plano09/ (README, MEMORY, fase-01 a fase-05). Sizing total 6h (bate com PLAN.md); wall-clock ~5h com fase-02‖fase-03 paralelizáveis. Ordem: fase-01 (bump) → fase-02‖fase-03 → fase-04 (sync) → fase-05 (rollback + tag). 6 ambiguidades sinalizadas: 09-A1 Plano 09 hard-bloqueia se Plano 07 incompleto (alternativa /todo-pick em v6.0.1 rejeitada), 09-A2 migration guide ≈30 linhas em 5 passos numerados (detalhe em docs/UPGRADE.md de Plano 08), 09-A3 sync-to-global NÃO copia docs/ (dog-food interno, não distribuível), 09-A4 manifest updateStrategy = replace para skills/hooks/scripts/libs, AGENTS.md/ARCHITECTURE.md FORA do manifest, 09-A5 rollback test em 3 níveis (mecânico/estrutural/funcional sanity sem /init real em CI), 09-A6 release date via sed em fase-05 antes do tag. Cross-cutting: Plano 03 (CHANGELOG depende da estabilização do --dry-run output), Plano 04 (scripts package.json adicionados pelo Plano 04 fase-05, não aqui), Plano 07 (path skills/todo-pick/SKILL.md hardcoded no manifest — rename quebra fase-03). Riscos novos: R-G10 (sync hardcoda /c/Users/luizf — outros devs override via PLUGIN_GLOBAL env), R-Plano08-cascade (dog-food falho bloqueia release — gate em fase-05 Passo 1), R-tag-bypass (force push proibido inline). **TODOS OS 9 PLANOS AGORA DETALHADOS. Pronto para /execute-plan.**
- 2026-05-11: Plano 08 detalhado gerado por subagente isolado. 10 arquivos em plano08/ (README, MEMORY, fase-01 a fase-08). Sizing total 14.5h (bate com PLAN.md); wall-clock ~11h com paralelismo (Wave 2: fases 04‖05‖06‖07). 7 ambiguidades sinalizadas: 08-A1 reuso de helpers Plano 03 via injection (sem duplicar), 08-A2 granularidade = 3 arquivos por PRD histórico (não 1 por sub-plano) com sub-secção "Phases History", 08-A3 CONTEXT-*.md soltos viram apêndice verbatim, 08-A4 TODO.md inicial com 3 itens reais, 08-A5 arquivos *.md soltos da raiz (78KB) DIFERIDO para TODO.md (não migrar em Plano 08), 08-A6 .github/workflows reusa template de Plano 02, 08-A7 AGENTS.md gerado manualmente (sem /init circular). Cross-cutting: helpers de Plano 03 (migrate-{planning,lessons,decisions}.ts) precisam aceitar paths absolutos arbitrários; harness:validate de Plano 04 deve aceitar plugin como projeto-alvo (fixture self-test); state-md-generator de Plano 06 precisa filtrar docs/ dentro de anti-vibe-coding/. Riscos novos: R-novo-1 (CLAUDE.md live), R-novo-2 (Windows symlink fail), R-novo-3 (10-seções placeholder em PRDs históricos), R-novo-4 (idioma misto PT/EN intencional), R-novo-5 (volume markdown fase-04 — validar CA-26 <2s). Próximo: criar Plano 09 (último) ou iniciar execução via /execute-plan.
- 2026-05-11: Plano 06 detalhado gerado por subagente isolado. 9 arquivos em plano06/ (README, MEMORY, fase-01 a fase-07). Sizing total ~12h (bate com estimativa do PLAN.md). DAG com 3 waves paralelas: Wave 1 (fase-01 ‖ fase-03 ‖ fase-07), Wave 2 (fase-02 após fase-01 ‖ fase-04 após fase-03), Wave 3 (fase-05 ‖ fase-06, ambas após fase-02). Wall-clock ~5-6h com 3 subagentes vs 12h serial. Decisão de design: fase-07 mantida no Plano 06 mas escopo reduzido a helpers TS (`lib/todo-utils.ts`) — skill `/todo-pick` nasce em Plano 07 fase-03 consumindo o helper. 9 ambiguidades sinalizadas: 06-A1 lock em `~/.claude/cache/state-md-last-run.json` chaveado por projectRoot, 06-A2 YAML completion signal strings simples (blocks_for_user trunca em 80 chars), 06-A3 Recent Activity top 5 por mtime com tie-break alfabético, 06-A4 case-sensitive `TODO.md`, 06-A5 slug `{original}-superseded` para nova ADR no revoke, 06-A6 fase-07 helpers-only, 06-A7 completion signal emitido em todas operações CRUD, 06-A8 `_archived/` separado em STATE.md (não soma com active), 06-A9 parsing YAML pending-capture só em `active/`. Cross-cutting: `gray-matter` provavelmente nova dep (verificar antes de fase-03); lock file global extensível para outros hooks futuros via `lib/state-md-lock.ts`; ADR revoke não idempotente (skill UI deve checar status superseded antes). Próximo: criar Plano 07, ou iniciar execução via /execute-plan.
- 2026-05-12: Execução iniciada — Plano 01. **Fase-01 (templates base) concluída** por plan-executor isolado. 3 arquivos criados em `anti-vibe-coding/skills/init/assets/templates/`: AGENTS.md.tpl (33 wc-l, 32 content lines, 8 linhas de margem para CA-27), ARCHITECTURE.md.tpl (35 linhas), README.md (11 linhas). Verificações OK: zero acentos PT, 3 links obrigatórios presentes, 2 placeholders ({{PROJECT_NAME}}, {{STACK}}) presentes, H1 em ARCHITECTURE.md.tpl. Test baseline pré-existente: 222 pass / 2 fail (failures em `skills/lib/profile-md-generator.test.ts` — commit 42acd02, sem relação com fase-01). DEV-01: TDD RED/GREEN ceremony skipped (infra não existe ainda). DEV-02: scripts/check-template-line-count.ts não criado (fase posterior). Decisão DI-02: `[ARCHITECTURE.md](./ARCHITECTURE.md)` aparece 2x no conteúdo (linhas 10 + 14) — coerente com spec content; critério `grep -c == 1` da spec é auto-contraditório, conteúdo > assertion.
- 2026-05-12: **Fase-02 (init skeleton) concluída** por plan-executor isolado (commit 8d11866). 4 arquivos criados em `anti-vibe-coding/skills/init/lib/`: scaffold-templates.ts (45 lines), scaffold-templates.test.ts (34 lines, 3 testes), detect-project-name.ts (8 lines), detect-project-name.test.ts (12 lines, 2 testes). SKILL.md modificado (+26 lines — Step 1 v6.0.0 antes do Passo 0 legacy). TDD: RED confirmado (`Cannot find module`) → GREEN confirmado (3 pass, 0 fail). Verificações OK: `bun run typecheck` limpo, `grep -L '{{'` retorna ambos arquivos, `wc -l AGENTS.md` = 33 (≤40). Decisão DI-03: scaffold-templates renderiza 6 placeholders (não só 2 da spec) com `"TBD"` default para os 4 extras de ARCHITECTURE.md.tpl — exigência do critério de aceite (`grep -L '{{'`). Desvio DEV-04: detect-project-name.test.ts criado por exigência do hook `tdd-gate.cjs` (GT-03). Novos gotchas: GT-03 (tdd-gate ativo), GT-04 (bun -e + paths absolutos quebra em Windows), GT-05 (/tmp resolve para \tmp\ na raiz do drive em bun/bash Windows).
- 2026-05-12: **Plano 01 CONCLUÍDO** (tracer bullet validado). Resumo: 5/5 fases done, 4 commits (8d11866, 00d3d31, f44c584, dab4bad), 0 bugs, 0 retries, 6 decisões registradas (DI-01..06), 5 desvios documentados (DEV-01..05), 7 gotchas descobertos (GT-01..07). Validação empírica em Win11 Pro sem devmode: **tier=hardlink confirmado** (R1 mitigada), test:tracer 287ms (alvo <30s), CA-26 (validator <2s) OK em 122ms, CA-27 (linecount) confirmada via regression test. Suite total: 233 pass / 2 fail (mesmo baseline pré-existente em profile-md-generator.test.ts — sem relação com Plano 01). D2 (EN-only), D13 (TS+bun), D16 (CLAUDE→AGENTS link) end-to-end OK. **Próximo:** Plano 02 (Full Scaffold) já detalhado (8 arquivos em plano02/), pronto para execução. Notas de continuidade gravadas no MEMORY.md do plano 01 (seção "Notas para o Plano 02").
- 2026-05-12: **Fase-03 (symlink fallback 3-tier) concluída** por plan-executor isolado (commit f44c584). 3 arquivos criados em `anti-vibe-coding/skills/init/`: lib/symlink-fallback.ts (81 lines), lib/symlink-fallback.test.ts (46 lines, 3 testes), assets/hooks/sync-agents-to-claude.cjs (30 lines). SKILL.md modificado (+24 lines — Step 2 v6.0.0 após Step 1). TDD: RED (`Cannot find module`) → GREEN (3 pass, 0 fail). Suite total: 231 pass / 2 fail (baseline inalterado). Validação empírica em Windows 11 sem developer mode: **tier=hardlink confirmado**, diff(AGENTS.md, CLAUDE.md) vazio, idempotência confirmada (2ª chamada também hardlink). Decisão DI-06: SKILL.md Step 2 documenta o passo via bloco bash/js — runner futuro importa libs direto (GT-04). Desvio DEV-05: 4º teste com mock EPERM omitido — bun:test não suporta override parcial de node:fs ESM (follow-up via DI pattern em Plano 04). Novo gotcha GT-07: tdd-gate.cjs bloqueia Write de .ts globalmente (inclusive f:/tmp/) — usar `.mjs` ou heredoc para scratch.
- 2026-05-12: **Fase-04 (validator minimal) concluída** por plan-executor isolado (commit 00d3d31), executada antes da fase-03 para evitar conflito de edits em scaffold-templates.ts. 3 arquivos criados em `anti-vibe-coding/`: skills/init/assets/templates/scripts/harness-validate.ts.tpl (89 lines, 3 checks paralelos via Promise.all), skills/init/assets/templates/package.json.tpl (14 lines, script harness:validate), tests/harness-validate.test.ts (62 lines, 3 testes via spawn). scaffold-templates.ts modificado (+5 lines: 2 pairs + mkdir recursivo); scaffold-templates.test.ts ajustado (length 2→4). TDD: RED 0/3 fail (ENOENT no .tpl) → GREEN 3/3 pass [536ms]. Verificações OK: typecheck limpo, full suite 228 pass / 2 fail (mesmo baseline pré-existente), CA-26 perf 536ms (<2s), CA-27 (line count) validado pelo teste automatizado. Decisão DI-05: contrato de `filesWritten` cresce conforme novos pairs — Plano 02 deve sincronizar. Novo gotcha GT-06: tdd-gate ignora `.tpl`/`.cjs`/`.json.tpl` — só bloqueia `.ts` puro (relevante para fase-03 que cria hook `.cjs`).
- 2026-05-12: **Plano 02 fase-02 (init-full-scaffold) concluída** por plan-executor isolado (commit 1ed7bc9). 2 arquivos criados em `anti-vibe-coding/skills/init/lib/`: scaffold-full-tree.ts (58 lines) + scaffold-full-tree.test.ts (65 lines, 4 testes). 3 arquivos modificados: scaffold-templates.ts (+`{{TODAY}}`), SKILL.md (Step 1 v6.0.0 chama `scaffoldFullTree`), tests/e2e/init-tracer-bullet.test.ts (assertion >=27 + chamada da nova helper). TDD: RED `Cannot find module './scaffold-full-tree'` → GREEN 4 pass / 0 fail (115ms). Tracer regression: 2 pass / 0 fail (291ms, tier hardlink). Typecheck limpo. Full suite **241 pass / 2 fail** (baseline preservado). Decisão DI-2: `Promise.all` + `mkdir({recursive:true})` paralelo é seguro (atomicidade `mkdirat`). 0 bugs, 0 gotchas novos, 0 desvios — implementação word-by-word da spec. Próximo recomendado: fase-06 (stack-detection) antes de fase-03, conforme ordem 01→02→06→(04‖05)→03 documentada no README.
- 2026-05-12: **Plano 02 fase-06 (stack-detection) concluída** por plan-executor isolado (commit f137533). 5 arquivos criados em `anti-vibe-coding/skills/init/`: lib/detect-stack.ts (109 lines), lib/detect-stack.test.ts (71 lines, 8 testes), lib/state-md-init.ts (60 lines), lib/state-md-init.test.ts (66 lines, 5 testes), assets/templates/docs/STATE.md.tpl (17 lines). 3 arquivos modificados: lib/template-manifest.ts (entry 26 — STATE.md.tpl), lib/scaffold-full-tree.ts (campo opcional `detectedStack` + placeholder `{{DETECTED_STACK}}`), SKILL.md (Step 3 v6.0.0). TDD: RED → GREEN 8/8 + 5/5 pass. Verificações OK: typecheck limpo, full suite **254 pass / 2 fail** (baseline +13 dos novos testes, mesmos 2 fails pré-existentes). 0 bugs, 0 desvios. Decisões: DI-01 `as object` documentado para spread de unknown JSON.parse, DI-02 `detectedStack` opcional backward-compat, DI-03 entry 26 inserida antes de TODO.md.tpl (camada "state snapshot"). Novo gotcha GT-08: `template-manifest.test.ts` checa diacriticos PT-BR em TODOS os templates — quebra silenciosa se alguem mergear `.tpl` com acentos. Próximo recomendado: fase-04 ‖ fase-05 (paralelizáveis) ou fase-03 (consome DetectedStack agora disponível).
- 2026-05-12: **Plano 02 fase-04 (GH Actions + PR template) concluída** por plan-executor isolado (commit 78d4823). 4 arquivos criados em `anti-vibe-coding/skills/init/`: assets/static/.github/workflows/harness.yml (bun-based GH Actions com `oven-sh/setup-bun@v2`), assets/static/.github/pull_request_template.md (Summary/Plan/Validation/Docs/Risk checklist com `bun run`), lib/install-gh-files.ts (cópia direta de 2 static assets), lib/install-gh-files.test.ts (4 testes: writes 2 files, CA-12 grep, no npm leftover, no tabs). SKILL.md modificado (+Step 5 v6.0.0 + comentário HTML placeholder reservado para Step 4 da fase-03). TDD: RED `Cannot find module './install-gh-files'` → GREEN 4 pass / 0 fail (58ms). Full suite **258 pass / 2 fail** (baseline +4 dos novos testes). Typecheck limpo. 0 bugs, 0 gotchas, 0 desvios — implementação word-by-word da spec. Decisões: DI-6 placeholder HTML para Step 4 (fase-03 substitui depois), DI-7 SKILL.md mantém `bun run -e` conforme spec (apesar de DI-06 do Plano 01 preferir import direto — fase é canon, refactor coletivo futuro).
- 2026-05-12: **Plano 02 fase-05 (Delivery Loop opt-in) concluída** por plan-executor isolado (commit fd506b5). 3 arquivos criados em `anti-vibe-coding/skills/init/`: lib/inject-optional-section.ts (genérico filePath/marker/body com idempotência), lib/inject-optional-section.test.ts (4 testes: injects, idempotent, marker-missing, marker preserved), assets/snippets/delivery-loop.md (5 linhas EN: Loom + Linear + Ready-for-Review). 2 arquivos modificados: assets/templates/AGENTS.md.tpl (expandido com `## Required Working Rules` + marker `<!-- INIT:DELIVERY_LOOP_SLOT -->` + `## Pre-Mutation Gate` — DEV-1; 31 wc-l sem opt-in / 39 com opt-in, ambos ≤40 CA-27 OK), SKILL.md (+Step 6 v6.0.0). TDD: RED `Cannot find module` → GREEN 4 pass / 0 fail. Full suite **262 pass / 2 fail**. Tracer bullet: 2 pass 0 fail (sem regressão). Decisões: DI-8 `## Conditional Reads` omitida do AGENTS.md.tpl para preservar budget de linhas (CA-27); DI-9 snippet condensado de ~8 para 5 linhas. Desvios: DEV-1 (template expandido com seções âncora — necessário para marker funcionar), DEV-2 (snippet de 5 linhas em vez de 8 — budget CA-27). Novos gotchas: GT-3 `bun run -e` retorna menu de ajuda em Windows bash com quoting ruim (workaround `bun --eval` ou script file), GT-4 `grep -P` warning de locale mas funciona para zero-match.
- 2026-05-12: **Plano 02 fase-03 (customizacao ARCHITECTURE.md) concluída** por plan-executor isolado (commit 045e617). 2 arquivos criados em `anti-vibe-coding/skills/init/lib/`: customize-architecture.ts (hash map STACK_PRESENTATION com 6 ids + substituição de marker), customize-architecture.test.ts (5 testes: 4 paramétricos por stack + 1 no-op marker-missing). 2 arquivos modificados: assets/templates/ARCHITECTURE.md.tpl (marker `<!-- INIT:STACK_BLOCK -->` logo após H1 — DI-10), SKILL.md (placeholder substituído por Step 4 real). TDD: RED `Cannot find module` → GREEN 5/5 pass (97ms). Full suite **267 pass / 2 fail** (baseline preservado). Typecheck limpo. 0 bugs, 0 desvios. Decisões: DI-10 marker logo após `# Architecture` em vez da posição idealizada da spec (template real não tem "Project: ..." nem "## Boundaries"). Novo gotcha GT-5: specs idealizadas podem divergir do estado real — subagentes devem inspecionar antes de seguir literalmente.
- 2026-05-12: **Plano 02 CONCLUÍDO** (Full Scaffold). 6/6 fases done, 6 commits (53c0070, 1ed7bc9, f137533, 78d4823, fd506b5, 045e617). Suite final: **267 pass / 2 fail** (baseline preservado). 10 decisões registradas (DI-1..10), 1 fase com 2 desvios (fase-05: DEV-1 expandiu AGENTS.md.tpl com seções âncora; DEV-2 snippet condensado), 5 gotchas novos (GT-1..5). Entregas: 25 templates `.tpl` em EN, 2 static assets `.github/`, 1 snippet `delivery-loop.md`, 8 helpers TS em `lib/` (scaffold-templates, scaffold-full-tree, detect-project-name, detect-stack, state-md-init, customize-architecture, install-gh-files, inject-optional-section), SKILL.md com Steps 1-6 v6.0.0. `/init` agora gera >=30 arquivos em fixture vazia. **Próximo:** Plano 03 (Migration v5→v6) já detalhado, pronto para execução. DAG documentado em plano03/README.md: 01→02→(03‖04‖05)→06→07. Notas de continuidade detalhadas registradas em plano02/MEMORY.md (seção "Notas para Plano 03").
- 2026-05-12: **Plano 03 fase-07 (E2E migration test) concluída** por plan-executor isolado (commits 5984def, 2a20c50). 12 arquivos criados: `tests/fixtures/legacy-v5/` (snapshot completo — .gitignore + .planning/{CONTEXT-baseline,2026-04-21-feature-x/{PRD,PLAN,STATE,plano01/{README,MEMORY,fase-01-baseline}}}.md + lessons-learned.md + decisions.md + senior-principles.md) e `tests/e2e/migration.test.ts` (5 testes ativos + 1 describe.skip aguardando Plano 04). SKILL.md modificado: Step migrate.5 adicionado (final validation + rollback hint + suggested commit). TDD: RED → GREEN 5 pass / 1 skip / 0 fail. Typecheck limpo. 0 bugs, 0 desvios materiais, 0 gotchas novos — implementação word-by-word da spec. CA-09 + CA-10 + M8 + CA-36 (backup integrity) todos validados por assertion. GT-12 confirmado (test rodado com escopo explícito: `bun run test tests/e2e/migration.test.ts`). **PLANO 03 CONCLUÍDO** (7/7 fases). Próximo: Plano 04 (Validators Full) já detalhado em plano04/, pronto para execução.
- 2026-05-12: **Plano 03 fase-06 (dry-run-mode) concluída** por plan-executor isolado (commit 5f96fbe). 6 arquivos criados em `anti-vibe-coding/skills/init/lib/`: dry-run.ts (WriteRecorder + makeWriter factory), dry-run.test.ts (4 testes), dry-run-renderer.ts (git diff --stat style), dry-run-renderer.test.ts (4 testes), migrate-orchestrator.ts (Promise.all sobre fase-03/04/05, propaga dryRun), migrate-orchestrator.test.ts (5 testes: CA-10 zero side effects, recordedWrites, real apply, renderer, timing). SKILL.md modificado: Steps migrate.0 (parse `--dry-run`) e migrate.all (orchestrated migration) adicionados antes do migrate.1. TDD: RED `Cannot find module` → GREEN 13/13 pass. Full suite **349 pass / 2 fail** (baseline +13 dos novos testes, mesmos 2 fails pré-existentes). Typecheck limpo. **Desvios materiais documentados:** DEV-06-01 (`recordedWrites` via soma de `written[]` dos helpers, não via WriteRecorder — helpers só chamam `writeFile` injetado quando `!dryRun`, então recorder ficaria vazio; soma é equivalente e mais direta); DEV-06-02 (orquestrador cria staging backup transiente em dry-run para os helpers conseguirem ler fonte `.planning.v5-backup/`, e remove via `finally` — preserva CA-10 do POV externo: docs/ nunca criado, .planning/ nunca tocado, backup só persiste se pré-existia). 1 gotcha novo: GT-13 (`tdd-gate.cjs` exige `.test.ts` co-localizado mesmo para renderers — DI-03 reaplica). Decisões: DI-10 (dry-run renderer estilo `git diff --stat` por linhas, plano-A-G4), DI-11 (staging backup transiente para preservar G1+CA-10). Próximo: fase-07 (E2E migration test — TRACER BULLET do Plano 03, sizing 2h).
- 2026-05-12: **Plano 03 fases 03+04+05 concluídas em paralelo (commits 8ee8ad1, 3fc1c6b, 2907010)**. Pre-flight do orquestrador: `lib/slugify.ts` + `slugify.test.ts` criados antes do paralelismo (6 testes) para eliminar race entre fase-04 e fase-05. 3 plan-executors disparados em paralelo, instruidos a NAO tocar SKILL.md e NAO commit (CLAUDE.md do submodulo documenta lição "Agentes paralelos com git add simultaneo agrupam commits de fases distintas"). Subagentes criaram 12 arquivos (fase-03: parse-planning-entry+migrate-planning, fase-04: parse-lessons+migrate-lessons, fase-05: parse-decisions+migrate-decisions, cada par com .ts+.test.ts). Orquestrador depois aplicou Steps migrate.2/3/4 no SKILL.md (1 Step por fase, 1 commit por fase, staging seletivo via `git add` explicito). Full suite **336 pass / 2 fail** (baseline preservado, +52 testes: 6 slugify + 15 fase-03 + 14 fase-04 + 16 fase-05). Typecheck limpo. 0 bugs, 0 retries, 0 desvios materiais. Decisões: fase-03 DI-01/02/03 (path separator normalization cross-platform — `split(/[\\/]/)` + `toForwardSlashes`); fase-04 DI-1/2/3 (regex strip CORRIGIDO/non-greedy mapping/loop sequencial G11); fase-05 DI-01/02/03 (10 testes em vez de 9, helpers exportados, inferTags privado). Notas para fase-06: 3 helpers (migratePlanning, migrateLessons, migrateDecisions) já aceitam `writeFile` injection — refactor para dry-run em fase-06 é cosmético (centralizar WriteRecorder + diff renderer). Para fase-07 (E2E): GT-12 do fase-02 continua válido — `bun run test` retorna exit 1 por causa de profile-md-generator pre-existing fails; o E2E deve filtrar caminho explicito. Próximo: fase-06 (dry-run-mode, ~1.5h, cross-cutting).
- 2026-05-12: **Plano 03 fase-02 (backup-idempotente) concluída** por plan-executor isolado (commit 488970c). 4 arquivos criados em `anti-vibe-coding/skills/init/lib/`: backup-planning.ts (helper atomico+idempotente+lock), backup-planning.test.ts (8 testes: creates, idempotent, nested structure, dryRun, cleans tmp, lock rejects, lock removed on success, read-only sobre source), copy-recursive.ts (wrapper paralelo com Promise.all), copy-recursive.test.ts (2 testes — necessario por tdd-gate). SKILL.md modificado: Step migrate.1 inserido apos Step 0.5 (bloco JS com import direto + gate de abort em erro). TDD: RED `Cannot find module` → GREEN 8/0 + 2/0 (+10 testes). Full suite **284 pass / 2 fail** (baseline preservado, +10 dos novos). Typecheck limpo. 0 bugs, 0 desvios. Decisoes: DI-01 SKILL.md usa `javascript` block com import direto (herdado de fase-01); DI-02 `copy-recursive.test.ts` criado por exigencia do tdd-gate (instrucao "tente sem teste auxiliar" inviavel — gate verifica path ANTES de Write). Novo gotcha GT-12: `bun run test` retorna exit 1 mesmo com nossos testes passando porque os 2 fails pre-existentes em profile-md-generator.test.ts continuam — relevante para fase-07 (E2E) e qualquer CI step `&& ...`. Proximo: fase-03/04/05 paralelizaveis (escrevem em sub-arvores disjuntas de `docs/`).
- 2026-05-12: **Plano 03 fase-01 (detector-v5) concluída** por plan-executor isolado (commit 1297cdd). 2 arquivos criados em `anti-vibe-coding/skills/init/lib/`: detect-v5-legacy.ts (heuristica + `LegacyArtifact` + `LegacyState` + `detectV5Legacy(targetDir)`), detect-v5-legacy.test.ts (7 testes parametricos: empty dir, .planning com conteudo, .planning vazia ignorada, lessons-learned isolado, 4 artefatos juntos, alreadyMigrated quando `docs/exec-plans/` existe, paths absolutos). SKILL.md modificado: Step 0.5 inserido antes do Step 1 (bloco JS com import direto + comentario read-only). TDD: RED `Cannot find module` → GREEN 7/7 pass. Full suite **274 pass / 2 fail** (baseline preservado, +7 dos novos testes). Typecheck limpo. 0 bugs, 0 desvios materiais. Decisoes: DI-01 SKILL.md Step 0.5 usa `await import('./lib/detect-v5-legacy.ts')` em vez de `bun run -e` (alinha com DI-06 do Plano 01 + GT-04 Windows); DI-02 comentario HTML `<!-- read-only -->` no Step 0.5 documenta G1 do plano. Helper read-only confirmado (zero mutacoes em disco). Proximo: fase-02 (backup-idempotente, 1.5h, depende de fase-01).
- 2026-05-12: **Plano 04 CONCLUÍDO** (Validators Full). 5/5 fases, 5 commits (c840ae7, 20eaadc, f114f7b, aecb0f1, d9f28be). Fases 04 e 05 executadas em paralelo (sem overlap de arquivos). Entregas: 2 validators TS+bun (`harness:validate` full + `compound:check`), 4 helpers (`compound-files-collector`, `compound-frontmatter`, `orphan-plan-detector`, `install-gh-files` ja existia), fixture sintetica `tests/fixtures/compound-100-docs/` + gerador, bench em `tests/perf/`, workflow GH Actions atualizado com 2 steps, package.json com 3 scripts (compound:check, harness:all, harness:validate). 11 decisoes (DI-01-*..05-*), 1 bug latente fixado (BUG-04-01 — frontmatter conflitava com H1 check), 6 gotchas (GT-01..06), 8 desvios DEV-*. CA-26 validado: harness:validate 88ms mediana, compound:check 90ms mediana (alvo <2000ms — margem 20x). Tracer bullet regressao OK. Typecheck limpo. 25 required-files canonicos definidos. **Próximo:** Plano 05 (Skill Migration + Hooks) já detalhado em plano05/, pronto para execução. Notas para Plano 05 + 06 + 08 + 09 gravadas em plano04/MEMORY.md "Notas para Planos Seguintes".
- 2026-05-12: **Plano 05 fase-02 hardening pos-audit** (commit 14cb235). Aplicados 4 fixes do security-auditor + code-smell-detector: (1) `writeFile` com flag `'wx'` em adr-writer.ts (race 02-G1 vira EEXIST detectavel em vez de overwrite silencioso); (2) `JSON.stringify(status)` no frontmatter (previne injecao YAML por caller JS untyped); (3) `replace(/\r?\n/g, ' ')` em `opts.title` antes da interpolacao em `decisions.md` legado (previne H2 espuria); (4) extraidas constantes `ADR_ID_WIDTH = 4` e `SLUG_MAX_LEN = 50` com WHY comments. +2 regression tests (status injection + title newline). Suite escopada: 21 pass / 0 fail (era 19/0). Typecheck limpo. 4 decisoes novas (DI-02-03..06). Race 02-G1 segue aceita como trade-off single-user — agora apenas fail-fast em vez de silent.
- 2026-05-12: **Plano 05 fase-02 (decision-registry migracao) concluída** por plan-executor isolado (commit 51bd334). 4 arquivos criados em `anti-vibe-coding/`: `skills/lib/adr-writer.ts` + `.test.ts` (9 testes — monotonic, frontmatter, slug, sections, collision, idempotence, pad4, defaults, mkdir), `skills/decision-registry/index.ts` + `.test.ts` (10 testes — CA-15 E2E, sequence, string form, ADRInput form, defaults, v5 fallback, header, append, no-v6-struct, cru). 1 modificado: `skills/decision-registry/SKILL.md` (+bloco "Fluxo (v6)" com pseudo-codigo dispatch v6/v5/cru). TDD: RED → GREEN 19 pass / 0 fail. Typecheck limpo. CA-15 verbatim validado: `add('Decidi usar Y', tmpDir)` em projeto v6 cria `docs/design-docs/ADR-0001-{slug}.md`; 2x chamadas → ADR-0001 + ADR-0002 monotonicos. Decisoes: DI-02-01 (paths em `skills/lib/` aplicando DI-01-01 do fase-01 — imports `'../lib/'` UM nivel), DI-02-02 (guard `m[1] != null` antes de parseInt — fix TS2345 sem `as`). 0 bugs, 0 desvios. Novo gotcha GT-novo-01: RegExp capture groups `m[1]` tipo `string | undefined` em TS strict — `parseInt` rejeita undefined, precisa guard adicional. 02-G1 race condition aceita como trade-off single-user (per spec). Disponivel para fases 03-08: `writeADR(designDocsDir, input)` em `skills/lib/adr-writer.ts`; assinatura D10 `add(arg, projectRoot)` consolidada em duas skills (lessons-learned + decision-registry). Proxima fase recomendada: fase-03 (plan-feature template harmonizado, ~2h, depende de fase-02).
- 2026-05-12: **Plano 05 fase-01 (lessons-learned migracao) concluída** por plan-executor isolado (commit 5ff8bb8). 8 arquivos novos em `anti-vibe-coding/`: `skills/lib/path-resolver-v6.ts` (61 lines) + `.test.ts` (4 testes), `skills/lib/compound-note-writer.ts` (98 lines) + `.test.ts` (5 testes), `skills/lessons-learned/index.ts` (57 lines) + `.test.ts` (6 testes), `tests/lessons-learned-v6.test.ts` (2 testes integracao CA-14 + D10), `tests/fixtures/v6-empty/` (4 .gitkeep para compound/design-docs/exec-plans/active/exec-plans/completed). 1 arquivo modificado: `skills/lessons-learned/SKILL.md` (+bloco "Fluxo de Captura (v6)" com pseudo-codigo executavel). TDD: RED `Cannot find module '../skills/lessons-learned/index'` → GREEN 17 pass / 0 fail nos escopos novos + 2/0 integracao. Validacao: typecheck limpo; tracer bullet Plano 01 sem regressao (2/0 pass). Decisoes: DI-01-01 helpers em `skills/lib/` (cross-skill, nao `anti-vibe-coding/lib/` inexistente), DI-01-02 fixture `v6-empty/` nova + legacy-v5/ reusada, DI-01-03 testes E2E criam tmp dirs proprios (nao mutam fixtures). 1 desvio: DEV-01-01 (path dos helpers — justificado em DI-01-01, aprovado pre-execucao). 0 bugs. 1 gotcha registrado: GT-novo-01 (`tests/fixtures/compound-100-docs/` artefato bench do Plano 04 — ignorar em git status). Disponivel para fases 02-08: `resolvePaths(root)`, `writeCompoundNote(dir, input)`, padrao D10 union signature, fixture v6-empty, padrao de tip de migracao idempotente.
- 2026-05-12: **Plano 04 fase-05 (CI + package.json compound:check)** concluída por plan-executor paralelo (commit d9f28be). 2 arquivos criados: `tests/workflow-yaml.test.ts` (5 testes) + `tests/package-json-scripts.test.ts` (4 testes). 3 modificados: `.github/workflows/harness.yml` (steps separados `Validate harness structure` + `Validate compound notes` com `time` + `::group::`; trigger inclui `master`), `package.json.tpl` (scripts `compound:check` + `harness:all`), `docs/PLANS.md.tpl` (secao Validators). TDD: 13 escopo / 0 fail. Decisoes: DI-05-actionlint (deferido), DI-05-workflow-path (`static/` em vez de `templates/`), DI-05-steps-separados (CI UI clarity vs `harness:all` local). 0 bugs, 0 desvios.
- 2026-05-12: **Plano 04 fase-04 (perf bench)** concluída por plan-executor paralelo (commit aecb0f1). 6 arquivos criados em `anti-vibe-coding/`: `tests/fixtures/generate-compound-fixture.ts` + `.test.ts` (5 testes), `.gitignore`/`.gitkeep` na fixture dir, `tests/perf/validators.bench.ts` + `.test.ts`. 2 modificados: `harness-validate.ts.tpl` (BUG-04-01 fix: SKIP_DIRS inclui `compound`), `tsconfig.json` (exclude fixture). TDD: 25 escopo / 0 fail. Tempos: harness 88ms mediana / compound 90ms mediana / aggregate 177ms (CA-26 PASS, margem 20x). Decisoes: DI-04-A3 (per-validator <2s), DI-04-A4 (mediana 3 runs apos warmup), DI-04-tsconfig (exclude fixture gerada). 1 bug fixado (BUG-04-01 — bug latente fase-03 descoberto no bench), 2 gotchas novos (GT-04 Defender warmup sem pico esperado, GT-05 TDD gate bloqueia geradores).
- 2026-05-12: **Plano 04 fase-03 (harness-validate advanced) concluída** por plan-executor isolado (commit f114f7b). 5 arquivos criados em `anti-vibe-coding/`: `skills/init/lib/orphan-plan-detector.ts` + `.test.ts` (8 testes), `tests/harness-validate-advanced.test.ts` (7 testes), 2 templates novos `assets/templates/README.md.tpl` + `assets/templates/.github/pull_request_template.md.tpl`. 3 modificados: `harness-validate.ts.tpl` inflado 90→200 linhas (25 required-files + link-checker + orphan-plan-detector + heading H1 + AGENTS required links), `template-manifest.ts` (27→29 entries), `tests/harness-validate.test.ts` (fixture atualizada para 25 required — GT-03 reaplicado). TDD GREEN: 8+7+3 escopo + tracer bullet 2/2 OK (regressao zero, 116ms). Typecheck limpo. **Decisoes:** DI-03-01 (README+pull_request_template como `.tpl` em vez de remover da REQUIRED — tracer confirmou), DI-03-02 (25 required-files v6 vs 22 do Andre — diferenca documentada: removidos `.mjs`, adicionados `.ts` + CLAUDE/COMPOUND/STATE). 0 bugs, 0 retries, GT-03 reaplicado (fixtures de fases anteriores atualizam quando validacao expande).
- 2026-05-12: **Plano 04 fase-02 (compound-check frontmatter+sections) concluída** por plan-executor isolado (commit 20eaadc). 3 arquivos criados em `anti-vibe-coding/`: `skills/init/lib/compound-frontmatter.ts` + `.test.ts` (17 testes), `tests/compound-check-frontmatter.test.ts` (5 testes E2E). 2 modificados: `scripts/compound-check.ts.tpl` (skeleton estendido com parser inline + sections check ~200 linhas), `tests/compound-check-skeleton.test.ts` (testes 3+4 atualizados para usar notas com frontmatter valido — DEV-02-01). TDD: RED → GREEN 17+5+4 pass (26 escopo). Typecheck limpo. **Ambiguidades resolvidas:** DI-02-A1 (H2 estrita case-sensitive, sem aliases — `^## (Problem|Solution|Prevention)\s*$`), DI-02-A2 (tags array nao-vazio = hard error), DI-02-G4 (parser inline regex em vez de gray-matter — sem dep nova). 1 novo gotcha GT-03 (fixtures placeholder de fases anteriores precisam atualizacao quando regras sao adicionadas). 0 bugs, 0 retries. Mensagens de erro em INGLES (G8) — pattern verbatim.
- 2026-05-12: **Plano 04 fase-01 (compound-check skeleton) concluída** por plan-executor isolado (commit c840ae7). 4 arquivos criados em `anti-vibe-coding/`: `skills/init/assets/templates/scripts/compound-check.ts.tpl`, `skills/init/lib/compound-files-collector.ts` + `.test.ts` (6 testes), `tests/compound-check-skeleton.test.ts` (4 testes E2E via spawn). 1 modificado: `lib/template-manifest.ts` (entry 27). TDD: RED `Cannot find module` + RED `ENOENT copyFile` → GREEN 6+4 pass. Typecheck limpo. Escopo dos testes respeitou GT-12 (sem `bun run test` global). 0 bugs, 0 retries, 3 decisoes (DI-01-01..03), 2 desvios (DEV-01-01..02), 2 gotchas novos (GT-01 Bun Dirent typing, GT-02 lint ausente — fallback typecheck-only). Lint ausente: nao bloqueia; `bun run lint` retorna "Script not found".
- 2026-05-12: **Plano 02 fase-01 (templates de todos os docs do harness) concluída** por plan-executor isolado (commit 53c0070). 27 arquivos criados em `anti-vibe-coding/skills/init/`: 25 templates `.tpl` em EN (8 docs institucionais + design-docs/ + exec-plans/ + compound/ + 6 review-checklists/ + smoke-flows/ + product-specs/ + references/ + generated/db-schema + TODO.md), `lib/template-manifest.ts` (manifest TS com TemplateEntry[]) e `lib/template-manifest.test.ts` (4 testes: count >=25, .tpl readable, no duplicate dst, no PT diacritics). TDD: RED `Cannot find module './template-manifest'` → GREEN 4/4 pass. Verificações OK: total tpl count 29 (4 pré-existentes + 25 novos), suite total 237 pass / 2 fail (baseline +4 dos novos testes, 2 fails pré-existentes preservados). Decisão DI-1: G-A1 e G-A2 confirmadas conforme assumido — `tech-debt-tracker.md.tpl` incluido, `FRONTEND.md`/`db-schema.md` como placeholders N/A. Novo gotcha GT-1 do plano02: `anti-vibe-coding/` é submódulo git — `git add`/`commit` devem rodar DE DENTRO do submodule (commit no repo raiz retorna "Pathspec is in submodule"). Próximo recomendado pelo agente: fase-02 (init-full-scaffold) — porém dev pediu parar após fase-01 para revisar.
- 2026-05-12: **Plano 05 fases-04 + 05 concluidas em paralelo** (commits 7204e34, 1120b96). 2 plan-executors disparados simultaneamente, instruidos a NAO commit (lição Plano 03). Fase-04 (quick-plan): 1 criado (`tests/quick-plan-template.test.ts`, 10 testes) + 1 modificado (`skills/quick-plan/SKILL.md` +Output Format v6 D18 reduzido); reusa `renderExecPlan({mode:'quick'})` de fase-03 — zero TS novo. Fase-05 (execute-plan): 5 criados (`skills/lib/exec-plan-reader.ts`+test, `skills/lib/exec-plan-mover.ts`+test, `tests/execute-plan-move.test.ts`; 28 testes total) + 1 modificado (`skills/execute-plan/SKILL.md` +Discovery v6 + Completion Flow antes do Step 0). Orquestrador stagiou + commitou cada fase separadamente via `git add` explicito. Suite full: **517 pass / 1 skip / 2 fail** (baseline preservado, +38 testes). Typecheck limpo. Decisoes: DI-04-01 (Output Format inserida antes de "O que Este Skill NAO Faz"); DI-05-01 (reader nao importa `path` — desnecessario); DI-05-02 (type guards em vez de `as` para mode/status do frontmatter); DI-05-03 (regex double-anchor `---\n...\n---\n` — planos sem frontmatter retornam defaults). Desvios: DEV-04-01 + DEV-05-01 (`index.ts` nao criado em quick-plan/execute-plan — markdown-only, herda DI-03-02), DEV-04-02 (sem RED real em fase-04 — renderExecPlan ja existia; GREEN imediato, justificado), DEV-04-03 (D10 string overload pertence a wrapper inexistente), DEV-05 (helper `onPlanPotentiallyComplete` nao criado — sugerido para `skills/lib/exec-plan-orchestrator.ts` se fase-06 precisar). Bug fixado: BUG-05-01 (`plans[0]` precisava guard `string|undefined`). Novos gotchas: GT-05-01 (`moveToCompleted` regex assume frontmatter na linha 1 — sem BOM strip; documentar), GT-05-02 (`resolvePaths` calcula `execPlansActiveDir` mesmo em v5/cru — helpers funcionam independente de layout). Notas para fases 06-08: `renderExecPlan({mode:'quick'})` validado em 10 cenarios; `readExecPlan(path)`/`isComplete(plan)`/`moveToCompleted(root,path)`/`listActivePlans(root)` disponiveis; telemetria `exec_plan.completed` ainda nao wired (fase-06 conecta); helper `onPlanPotentiallyComplete` para criar em `skills/lib/exec-plan-orchestrator.ts` (NAO em `index.ts` de skill). Proximo: fase-06 (iterate-compound-gate, 2h, depende de fase-01 + fase-05 — ambas done) ou fase-07 (pre-mutation-gate-hook, 2h, independente).
- 2026-05-12: **Plano 05 fase-06 + fase-07 concluidas (commits 2a7ba4f, fea14f3)**. Fase-06 disparada em paralelo via plan-executor isolado (concluiu 7 pass / 25 expects, typecheck limpo). Fase-07 disparada em paralelo MAS caiu com API error 529 (overloaded) apos completar apenas o RED test em disco. Resumed via segundo plan-executor — produziu GREEN completo (2 .cjs + hooks.json update; 8 pass / 18 expects). Suite somada das 2 fases: **15 pass / 0 fail / 43 expects**. **Fase-06 entregas:** `skills/lib/compound-decision-gate.ts` (131 lines, hash-map de handlers capture/no_capture_needed/postpone) + `.test.ts` (3 testes) + `tests/compound-decision-gate.test.ts` (4 testes E2E) + bloco "Compound Decision Gate (D17)" em `skills/iterate/SKILL.md`. **Fase-07 entregas:** `hooks/lib/heuristic-mutation.cjs` (78 lines — shouldSuggestPlan D26) + `hooks/pre-mutation-gate.cjs` (152 lines — UserPromptSubmit hook com lock TTL 5s, telemetria JSONL, CA-23/24) + entry em `hooks/hooks.json` apos user-prompt-gate. **Decisoes:** DI-06-01 (skills/iterate markdown-only — sem index.ts), DI-06-02 (appendValidationLog cria secao se ausente), DI-06-03 (spread condicional para exactOptionalPropertyTypes), DI-07-01 (shouldSuggestPlan **inlined** dentro do hook por bug do require() em paths Windows com espaco — helper segue como fonte de verdade nos unit tests). **Novos gotchas:** GT-06-01 (`exactOptionalPropertyTypes: true` quebra com `T | undefined` em props opcionais — usar spread condicional), GT-07-01 (`require()` Node nativo falha silenciosamente em paths Windows com espacos; Bun `createRequire(import.meta.url)` funciona via URL encoding). **Desvios:** DEV-06-01/02 (paths + sem index.ts), DEV-07-01/02 (inline duplication + async race guard). 0 bugs. CA-16 + CA-23 + CA-24 + CA-25 cobertos por assertion. Smoke manual do hook: `inject:true / block:false` para prompt substancial sem plano ativo. **Proximo:** fase-08 (zero-breaking-change-tests, tracer bullet CA-17, 1.5h) — agora desbloqueada (depende de fases 01-07, todas done).
- 2026-05-12: **Plano 05 CONCLUÍDO** (Skill Migration + Hooks). 8/8 fases, 8 commits (5ff8bb8, 51bd334, fd94646, 7204e34, 1120b96, 2a7ba4f, fea14f3, 1e737f0). Suite escopada fase-08: **24 pass / 0 fail**. Entregas: 4 wrappers D10 `index.ts` (plan-feature, quick-plan, execute-plan, iterate), fixture `tests/fixtures/v6-empty/`, helper `tests/helpers/v6-fixture-setup.ts`, `tests/zero-breaking-change.test.ts` (24 testes CA-17), `tests/setup.ts` + `bunfig.toml` (ANTI_VIBE_DISABLE_HOOKS=1), early-return em `hooks/pre-mutation-gate.cjs`. Decisoes: DI-08-01 (tdd-gate contornado via bash cat>, wrappers cobertos indiretamente pelo zero-breaking-change.test.ts), DI-08-02 (`import.meta.dir` em vez de `__dirname` — ESM Bun), DI-08-03 (fixture v6-empty ja tinha .gitkeep, READMEs adicionados). CA-17 verbatim validado. Typecheck limpo. **Proximo:** Plano 06 (Agent-Native D31/D32/D33).
- 2026-05-12: **Plano 08 fase-01 CONCLUÍDA** (audit D29 + backup + stubs). SHA snapshot: 1b8b376, SHA fase-01: efad3b1. 4 backups em `.planning.v5-backup/`, 5 stubs em `docs/` (PIPELINE, MODEL_PROFILES, AGENTS_LIST, UPGRADE, design-docs/core-beliefs), 20 items `confirmed` no audit table. CLAUDE.md intacto (346 linhas). Gotchas: `.planning/` não existia no plugin (criado clean), `docs/design-docs/` também novo. Próximo: fase-02 (AGENTS.md ≤40 linhas EN).
- 2026-05-12: **Plano 08 fase-02 CONCLUÍDA** (AGENTS.md plugin) — commit `00e519e` no submodulo `anti-vibe-coding/`. AGENTS.md ficou com 33 linhas EN (CA-27 OK, margem 7). Validacao via `bun run test tests/harness-validate-advanced.test.ts`: 7 pass / 0 fail (CA-27 incluido). Typecheck limpo. CLAUDE.md original intacto (G2). Decisoes: DI-05 (33 lines vs 32 do template — wc -l conta linha em branco final, dentro do limite). Desvios: DEV-1 (substituido CLI standalone `bun scripts/harness-validate.ts` — que eh `.tpl` — por teste advanced que cobre CA-27). Gotchas: GT-1 (EBUSY no Windows na 1a run, flake nao relacionado ao conteudo — rerun resolve). 0 bugs. Proximo: fase-03 (criar ARCHITECTURE.md + 8 docs institucionais, ~3h, serial pre-paralelismo fases 04/05/06/07).
- 2026-05-12: **Plano 08 fase-03 CONCLUÍDA** (ARCHITECTURE.md + docs institucionais) — commit `21b63bb` no submodulo `anti-vibe-coding/`. 28 arquivos novos/modificados (ARCHITECTURE.md + 8 docs institucionais EN: DESIGN/FRONTEND/PLANS/PRODUCT_SENSE/QUALITY_SCORE/RELIABILITY/SECURITY/COMPOUND_ENGINEERING + 4 stubs Camada 2 sobrescritos com conteúdo final traduzido de CLAUDE.md PT→EN: PIPELINE 67 linhas, MODEL_PROFILES com tabela 3 perfis + matriz por agente, AGENTS_LIST 13 agentes com coluna Default Profile, UPGRADE com seção v5.x→v6.0.0 + estruturas docs/{exec-plans/active,completed/,compound/,review-checklists/,smoke-flows/,product-specs/,references/,design-docs/,generated/} cada uma com README.md/index.md). Verificações: find docs -name "*.md" wc -l = 39 (≥22 OK), wc -l PIPELINE.md = 67 (≥30 OK), grep -c '|' MODEL_PROFILES.md = 20 (≥8 OK), 4 subdirs presentes (exec-plans/{active,completed}, compound, review-checklists). CLAUDE.md intacto (G2). core-beliefs.md de fase-01 preservado. Decisões: DI-01 (product-specs/index.md em vez de raiz), DI-02 (MODEL_PROFILES tabela do model-profiles.json real, não prosa CLAUDE.md). Gotchas: GT-01 (LFLF→CRLF warnings cosméticos Windows), GT-02 (15 arquivos pré-existentes em docs/ — v5.3 telemetry/baseline — não tocados). 0 bugs, 0 desvios. Próximo: fases 04/05/06/07 (100% paralelas — migrar planos históricos, lessons-learned, senior-principles, decisions).
- 2026-05-12: **Plano 07 CONCLUÍDO** (TODO.md + /todo-pick). 4/4 fases, 4 commits (8ec7c09 bundle fase-01+bugfix, 0002a0b fase-02, c4eb98e fase-04, 2be2b9d fase-03). Suite: 18 pass fase-03 + 14 fase-02 + 13 fase-01 + 7 fase-04 = 52 novos testes. BUG-01: `captureToTodoMd` double-prefix detectado e corrigido com teste regressao. Entregas: `skills/todo-pick/SKILL.md` (CA-31/CA-32/CA-44), `skills/todo-pick/templates/todo-md-skeleton.md` (EN), `skills/init/lib/scaffold-todo-md.ts` (idempotente), `skills/lib/todo-utils.ts` extendido (+parseLine/listPending/filterByStatus/pickNext/scoreByPriority), `skills/lib/execute-plan-todo-capture.ts` (formatTodoLine+captureToTodoMd CA-33), `skills/execute-plan/SKILL.md` (bloco captura out-of-scope), `plugin-manifest.json` (+skills.todo-pick provisorio). Decisoes criticas: DI-07 (plugin-manifest skills key provisoria), BUG-01 bypass addLine. Notas completas em plano07/MEMORY.md. **Proximo:** Plano 08 (Dog-Fooding, 8 fases).
- 2026-05-12: **Plano 06 CONCLUÍDO** (Agent-Native D31/D32/D33). 7/7 fases, 7 commits (ff93580, 0876e47, 1abf5c3, 777902a, e76468e, a68a6f6, ad22624). Suite global final: 626 pass / 3 fail (3 pre-existentes em profile-md-generator + pre-mutation-gate snapshots). Wave 1 (paralelo): fase-01 `skills/lib/completion-signal.ts` + teste (7 pass); fase-03 `skills/lib/state-md-generator.ts` + fixture + `skills/lib/state-md-lock.ts`; fase-07 `skills/lib/todo-utils.ts` + teste (16 pass). Wave 2: fase-02 6 skills emitem YAML completion signal via `renderCompletionSignal`, `tests/completion-signal-emission.test.ts` (7 pass), 6 `SKILL.md` atualizados. Wave 3 (paralelo fase-04 / sequencial 05+06): fase-04 `hooks/state-md-hook.cjs` PostToolUse com rate-limit 30s (lock inlined, GT-07-01); fase-05 `skills/lib/lessons-learned-crud.ts` `update()`+`archive()`+`normalizeDateFields()` (8 pass, DI-04, GT-04); fase-06 `skills/lib/decision-registry-revoke.ts` `revoke()` com ADR superseded bidirecional (10 pass, GT-05). Decisoes criticas: DI-06 (todos helpers em `skills/lib/`), DI-03 (lock inlined em CJS), GT-07-01 (Windows spaces). 1 bug (BUG-01 spyOn console.log). 2 desvios (DEV-01 fixture efemera, DEV-02 sync vs async). Notas completas em plano06/MEMORY.md. **Proximo:** Plano 07 (TODO.md + /todo-pick, 4 fases).
- 2026-05-12: **Plano 05 fase-03 (plan-feature template harmonizado D18) concluída** por plan-executor isolado (commit fd94646). 6 arquivos criados em `anti-vibe-coding/`: `skills/lib/exec-plan-sections.ts` + `.test.ts` (5 testes — FULL count/order, QUICK count/order, mode type guard), `skills/lib/exec-plan-template.ts` + `.test.ts` (13 testes — ordem exata 10 secoes full, 7 quick, frontmatter, heading), `skills/plan-feature/templates/exec-plan-full.md.tpl` (template raw EN com tokens `{{title}}`/`{{date}}`), `tests/plan-feature-template.test.ts` (6 testes E2E — `writeExecPlan` cria arquivo no path correto, 10 H2 ordem exata, frontmatter, slug). 1 modificado: `skills/plan-feature/SKILL.md` (+ secao "Output Format (v6 — D18)" antes de `## Regras`). TDD: RED `Cannot find module './exec-plan-sections'` (3 erros) → GREEN 25/0. Typecheck limpo. Suite escopada: 25/0. Suite full: **479 pass / 1 skip / 2 fail** (baseline preservado, +25 dos novos testes). Decisoes: DI-03-01 (SKILL.md sem "Output Format" pre-existente — inserida antes de `## Regras`); DI-03-02 (`plan-feature/index.ts` nao existe — skill markdown-only, confirmado em fase-01/02); DI-03-03 (`writeExecPlan` sem flag `'wx'` — filename deterministico permite overwrite em reruns de teste; diferente de ADR que tem numeracao monotonica); DI-03-04 (`bulletOrEmpty`/`checkboxOrEmpty` aceitam `readonly string[]` para receber arrays `as const`). Desvios: DEV-03-01 (path `skills/lib/` em vez de `anti-vibe-coding/lib/` — confirma CAVEAT DI-01-01/DI-02-01), DEV-03-02 (`writeExecPlan` colocado no mesmo arquivo de `renderExecPlan` em vez de arquivo separado — DRY/coesao). 0 bugs. Novo gotcha GT-03-01: `readonly string[]` necessario para aceitar arrays `as const` sem type assertion (proibido `as` per CLAUDE.md). API disponivel para fases 04-08: `renderExecPlan({mode, title, ...})`, `writeExecPlan(projectRoot, input)`, `EXEC_PLAN_SECTIONS_FULL/QUICK`, tipo `ExecPlanInput`. Proximo: fase-04 (quick-plan reusa `renderExecPlan({mode:'quick'})`) ou fase-05 (execute-plan paths novos) — ambas dependem desta.
- 2026-05-12: **Plano 08 fase-04 CONCLUÍDA** (migrar 3 PRDs históricos) — commit `80ee36e` no submódulo. 3 arquivos criados em `anti-vibe-coding/docs/exec-plans/completed/`: `2026-04-15-refatoracao-prd-folders.md` (363 linhas, 23 H2), `2026-04-21-anti-vibe-v52.md` (338 linhas, 16 H2), `2026-05-04-v53-plugin-adaptativo.md` (428 linhas, 22 H2). Total 1129 linhas — 3 PRDs históricos consolidados em D18 10-sections (Goal/Scope/Assumptions/Risks/Execution Steps/Review Checklist/Validation Log/Compound Opportunity/Lessons Captured/Exit Criteria) + appendix Phases History + CONTEXT verbatim. DI-09: helper `migrate-planning.ts` (Plano 03) NÃO utilizável para dog-food — hardcoda leitura de `${targetDir}/.planning.v5-backup/.planning/` e escreve em `active/`, não `completed/`. Fase-04 manual (Read+Write). 0 testes regressão (typecheck/test não rodam em arquivos docs/). CA-02 satisfeito (3 PRDs com 10 secoes obrigatorias).
- 2026-05-12: **Plano 08 fase-05 CONCLUÍDA** (lessons-learned → compound notes) — commit `0485da5` no submódulo. 5 arquivos criados em `anti-vibe-coding/docs/compound/`: `2026-03-23-hooks-json-overwrite-bug.md`, `2026-04-21-grep-c-exit-1-quando-zero.md`, `2026-04-21-anti-vibe-coding-git-independente.md`, `2026-04-21-blocos-codigo-aninhados-skill-md.md`, `2026-04-21-hooks-cjs-stdin-pattern.md`. Cada nota com frontmatter (title/category/tags/created — CA-29) + body Problem/Solution/Prevention (CA-14). DI-10: helper `migrate-lessons.ts` também incompatível (hardcoda `${targetDir}/.planning.v5-backup/lessons-learned.md` mas fase-01 backup usou sufixo `.original`). Fase-05 manual. GT-4: 6a lesson em lessons-learned.md (BUG-02 sobre blocos de código em prompts) e lesson sobre subagentes git race NÃO mapeadas em fase-05 spec — diferidas para fase-08 (junto com migração final / TODO). CA-03 satisfeito.
- 2026-05-12: **Plano 08 fase-06 CONCLUÍDA** (senior-principles → core-beliefs.md) — commit `aa28b20` no submódulo. 1 arquivo sobrescrito: `anti-vibe-coding/docs/design-docs/core-beliefs.md` (de stub 30 linhas para 152 linhas EN final, 10 seções, 8 skill pointers). Conteúdo: spec template body (linhas 44-192 fase-06.md) verbatim em EN + 2 itens detectados ao ler senior-principles.md L10 (Anti-Sycophancy) + CLAUDE.md L38-L41 (Type-safety/Monitoring/KISS bullets). DI-11: backticks REMOVIDOS de `Skill: /anti-vibe-coding:xxx` para texto puro — necessário para `grep -c 'Skill: /anti-vibe-coding:'` retornar >0. CA-15 (parcial — ADR vira em fase-07) e D29 #10 (core-beliefs canonical) satisfeitos.
- 2026-05-12: **Plano 08 fase-07 CONCLUÍDA** (decisions → ADR) — commit `bf8837e` no submódulo. 2 arquivos criados/modificados em `anti-vibe-coding/docs/design-docs/`: `ADR-0001-manifest-checksums.md` (NEW, 129 linhas com frontmatter status/date/deciders/superseded_by + 5 seções Context/Decision/Consequences/Alternatives/Verbatim + appendix do decisions.md verbatim) e `index.md` (REWRITTEN do table stub para seções `## ADRs` + `## Core Beliefs` linkando ambos). DI-12: helper `migrate-decisions.ts` mesma incompatibilidade dos outros 2 (.original suffix). Fase-07 manual. CA-15 satisfeito completamente. GT-5 consolidado: helpers `migrate-{planning,lessons,decisions}.ts` de Plano 03 incompatíveis com dog-food por (1) hardcodam `${targetDir}/.planning.v5-backup/{name}` sem `.original` suffix; (2) escrevem em `active/` não `completed/`. Trabalho manual nas 3 fases mais simples que adaptar helpers. Helpers funcionarão normalmente para outros projetos (backup padrão sem `.original`). **Pronto para fase-08 (validação final + delete legacy)**.
- 2026-05-12: **Plano 08 fase-08 CONCLUÍDA — Plano 08 INTEIRO CONCLUÍDO** (validação final + delete legacy + Tier 3 symlink fallback). Commits no submódulo: `dc05e63` (wip pre-delete-snapshot — validators green com legados ainda em raiz), `8cab16c` (delete CLAUDE.md/lessons-learned/senior-principles/decisions + criar Tier 3 copy CLAUDE.md=AGENTS.md + .github/workflows/harness.yml + .github/pull_request_template.md + docs/STATE.md + TODO.md + fix README.md broken links), `1d7e2db` (typecheck fix noUncheckedIndexedAccess guards em harness-validate.ts). 4 patches no validator durante dog-food (BUG-08-01..04): SKIP_DIRS estendido (compound/templates/__fixtures__), H1 check stripa frontmatter+HTML comments, SKILL.md isento, link checker guard. Sync `.tpl` ↔ runtime copy. Validação final: `harness:validate` exit 0 com 175 .md, `compound:check` exit 0 com 5 notes, typecheck clean, 682 tests pass / 1 skip / 3 baseline fail (no regressions). CLAUDE.md final = Tier 3 copy (Windows git-bash degrada Tier 1 silenciosamente — GT-6). DEV-2..4 documentados (patch validator não dados; Tier 3 não Tier 1; fix README links). TODO.md com 6 itens deferidos (incluindo hooks/agents-md-sync.cjs para resync automático). **Plano 08 completo 8/8, D20/R4 mitigado. Próximo: Plano 09 (Versioning & Release).**
- 2026-05-12: **/iterate Compound Decision Gate executado** sobre Plano 08. 2 compound notes capturadas em `anti-vibe-coding/docs/compound/`: `2026-05-12-dog-food-reveals-strict-validators.md` (BUG-08-01..05 + DEV-2 + GT-8) e `2026-05-12-tier1-symlink-silent-fail-windows.md` (GT-6 + DEV-3). Commit no submódulo: `fe6cd67`. compound:check exit 0 (7 notes total), harness:validate exit 0 (173 .md). **Plano 08 fechado com aprendizados durarveis preservados**. Próximo: Plano 09 Versioning & Release.
- 2026-05-12: **Plano 09 fase-01 CONCLUÍDA** (version bump 5.3.0 -> 6.0.0). Commit submódulo `00965ae`: `chore(release): bump version to 6.0.0`. 3 arquivos JSON modificados (package.json, .claude-plugin/plugin.json, plugin-manifest.json top-level), 1 backup criado em `.release-backup/v5.3.0/`, `.gitignore` ajustado. typecheck clean, bun test = 678 pass / 1 skip / 3 fail (baseline preservado; GT-03 registra que baseline real é 3 fail, não 2 como Plano 02 documentou). DI-01: descrição literal da spec (247 chars). 3 gotchas novos em plano09/MEMORY.md: GT-01 (git stash conflita com fixtures WIP — usar drop+reaplicar), GT-02 (jq ausente no PATH Windows — fallback `node -e`/`bun -e`), GT-03 (baseline drift de 2→3 failures).
- 2026-05-12: **Plano 09 fase-02 CONCLUÍDA** (CHANGELOG v6.0.0 section) — commit submódulo `67e1182`: `docs(changelog): add v6.0.0 section with breaking changes and migration guide`. Inserido bloco `## [6.0.0] - 2026-XX-XX` (~172 linhas, 7 subseções Keep-a-Changelog + Migration Guide com 5 Steps numerados) antes de `[5.3.0]` (DEV-01: spec previu insercao antes de `[4.0.0]`; CHANGELOG ja tinha `[5.3.0]` como entry mais recente). CHANGELOG final 390 linhas. DI-02 (link compound notes ajustado para `2026-04-21-grep-c-exit-1-quando-zero.md` — nome real no repo), DI-03 (linha em branco entre `### Breaking Changes` e primeiro item removida para satisfazer pattern do CA-35), DI-04 (Step 5 renomeado de `Step 5 (optional): Customize` para `Step 5: Customize (optional)` para regex `^#### Step [1-5]:`).
- 2026-05-12: **Plano 09 fase-03 CONCLUÍDA** (manifest regen) — commit submódulo `08243a5`: `chore(manifest): regenerate plugin-manifest.json for v6.0.0`. `scripts/generate-manifest.js` modificado substancialmente (177 → 236 linhas — DI-05) com 5 fixes (env PLUGIN_VERSION, scan scripts/skills-lib/config/init-assets, SKILL.md case-fix, IGNORED_PREFIXES dog-food, .test.cjs exclude). `scripts/__tests__/generate-manifest.test.ts` NEW (16 tests). Manifest cresceu 134 → 246 entradas (DEV: spec previu +10, realidade +112 porque gerador antigo under-counting). typecheck FAIL inicial em test:117 (Object possibly undefined) fixado em fix-pos-fase-03. 2 gotchas novos: GT-04 (skills top-level index é patch MANUAL de commit `2be2b9d`, qualquer regen DROPA silenciosamente — TODO durarvel: ensinar gerador a emitir skills), GT-05 (gerador antigo tinha 5 deficiências acumuladas).
- 2026-05-12: **Fix-pos-fase-03** — commit submódulo `2809d6b`: `fix(manifest): restore skills.todo-pick index + test guard after fase-03 regen`. BUG-01 documentado em plano09/MEMORY.md: fase-03 regen DROPOU `skills.todo-pick` (patch manual de `2be2b9d`), quebrando `tests/todo-pick.test.ts:61-82` (3 testes). Fix: (1) restore manual de `skills.todo-pick` no manifest, (2) guards `if (!relPath) return` + `if (entry)` no test, (3) TODO em `anti-vibe-coding/TODO.md` para fix durarvel. Resultado: typecheck clean, 694 pass / 1 skip / 3 fail (baseline preservado). Forward-fix escolhido em vez de Level-1 retry — subagente fez net-positive (corrigiu 5 bugs long-standing do gerador).
- 2026-05-12: **Plano 09 fase-04 CONCLUÍDA** (sync-to-global.sh path v6.0.0) — commit submódulo `cc1d6af`: `chore(sync): update sync-to-global.sh for 6.0.0 path`. Script reescrito 54 → 116 linhas (87 ins / 25 del). PLUGIN_GLOBAL 5.2.0 → 6.0.0 (env-overridable). Funcoes auxiliares `copy_dir_if_exists` + `copy_file_if_exists` tolerantes a paths ausentes. Adicionados paths v6: AGENTS.md, ARCHITECTURE.md, scripts/, .github/, tests/fixtures/, tsconfig.json, CHANGELOG.md, README.md. 3-tier CLAUDE.md→AGENTS.md symlink fallback. `set -u` mas NAO `set -e` (tolerancia). Idempotencia validada via sandbox (hash SHA256 identico apos 2 runs). Tolerancia a paths ausentes validada (exit 0 sem origens). DI-06: CA regex `! grep -qE 'rsync|realpath|readlink -f'` falha por match em comentario WHY (`cp -r em vez de rsync — rsync nao garantido em Git Bash Windows nativo`); excluindo comentarios, script eh POSIX-puro — CA satisfeito semanticamente.
- 2026-05-12: **Plano 09 fase-05 CONCLUÍDA — RELEASE v6.0.0 GA LOCAL**. 3 commits submódulo:
   - `fbbb84f`: `chore(release): finalize 6.0.0 release date` — sed `2026-XX-XX` → `2026-05-12` em CHANGELOG.md + scripts/sync-to-global.sh
   - `4c18844`: `test(release): add rollback validation against legacy-v5 fixture` — tests/rollback.sh (172 lines, 3 niveis: mecanico/estrutural/funcional) + tests/rollback-report.md (ALL CHECKPOINTS PASSED)
   - **Tag anotado `v6.0.0`** criado em `4c18844` com mensagem rica (titulo + Harness/Compound + rollback validated + CHANGELOG link). NAO pushado (autorizacao manual pendente).
   Rollback test: 9 checkpoints PASS (Level 1 mecanico + Level 2 estrutural 5x + Level 3 funcional 3x + Cleanup). Validado com REVERT_COUNT=6 em HEAD=fbbb84f; committed com default REVERT_COUNT=7. Pre-conditions OK: harness:validate exit 0 (173 .md), compound:check exit 0 (7 notes), typecheck clean, bun test 694 pass / 1 skip / 3 baseline fail. CA-36 satisfeito completamente. DI-07: spec loop `git revert HEAD~i` em sequencia causa oscilacao revert/reapply (HEAD muda apos cada revert); substituido por `git revert HEAD~N..HEAD` range syntax (git >= 1.7.2) que cria N revert commits individuais limpos. DI-08: REVERT_COUNT=7 covers todos os 7 commits do release; documentado inline para hotfixes futuros override via env var.
- 2026-05-12: **PLANO 09 INTEIRO CONCLUÍDO 5/5 — TODOS OS 9 PLANOS DO PRD v6.0.0 COMPLETOS (55/55 fases, 100%)**. v6.0.0 esta GA localmente. Proximo passo (manual, aguardando autorizacao do dev): `git push origin master` + `git push origin v6.0.0` no submodulo. Compound Decision Gate (post-Plano 09): considerar /iterate para capturar licoes do release (ex: spec do rollback test tinha bug de loop, generator antigo under-counting). PRD `2026-05-11-v60-harness-compound-fusion` pode ser movido para `_archive/` apos push.
