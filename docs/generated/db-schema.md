# Database Schema

Not applicable: this project has no database.

The plugin operates entirely on the local filesystem. No database is used.
All state is stored in markdown files under `docs/` and `~/.claude/projects/`.
