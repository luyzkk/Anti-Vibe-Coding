// STUB — RED phase only. Replace with real implementation in GREEN.
import type { Step } from './types'

export const backupPre650Step: Step = {
  id: '00_3-backup-pre-6_5_0',
  async run(_ctx) {
    return { mutated: true, summary: 'stub' }
  },
}
