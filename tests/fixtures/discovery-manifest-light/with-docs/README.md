# My Project

A sample project used as a fixture for discoveryManifestLight tests.

## Overview

This fixture contains a handful of markdown files spread across different
directories to verify that the scanner finds them all correctly.

## Usage

Run the tests with `bun test`.
