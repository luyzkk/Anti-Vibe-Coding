# TODO

- [x] {2026-05-12} {file:IMPLEMENTACAO-VERSIONAMENTO.md} consolidate IMPLEMENTACAO-VERSIONAMENTO.md + MUDANCAS-RECENTES.md + INSTRUCOES-SINCRONIZACAO.md + COMO-ATUALIZAR.md into docs/UPGRADE.md and docs/HISTORY.md (08-A5) — done 2026-05-13: 4 root v5 docs moved to docs/references/v5-legacy/ alongside 11 v5-era docs from docs/ root; UPGRADE.md remains canonical v6 upgrade guide; CHANGELOG.md covers v5→v6 history
- [x] {2026-05-12} {feature:plugin} verify hooks/state-md-hook.cjs is firing correctly on docs/ edits (G15) — done 2026-05-13: registrado em hooks/hooks.json L80-90 sob PostToolUse matcher `Write|Edit|MultiEdit`; wrapper inline usa env CLAUDE_HOOK_INPUT + h.handle() direto (correto para PostToolUse, bypassa stdin path); RELEVANT_PATTERNS cobre docs/compound/, docs/design-docs/ADR-*, docs/exec-plans/(active|completed)/, TODO.md root; rate-limit 30s via ~/.claude/cache/state-md-last-run.json; generator existe em skills/lib/state-md-generator.ts e invocado via spawnSync bun; 7/7 testes hooks/state-md-hook.test.cjs verdes
- [x] {2026-05-12} {feature:plugin} v6.1 — implement hooks/pre-tool-use-destructive-guard.cjs (D29 item 7, deferred from plano08-fase01 audit) — done 2026-05-13: hook em hooks/pre-tool-use-destructive-guard.cjs bloqueia (exit 2 + stderr) 7 padroes: rm -rf, git reset --hard, git push --force[-with-lease] em main/master, git clean -f/-fd, git branch -D, git checkout -- / restore . / checkout ., e --no-verify em commit/push; bypass via env AVC_ALLOW_DESTRUCTIVE=1; wired em hooks/hooks.json PreToolUse matcher Bash; smoke test em tests/hooks/pre-tool-use-destructive-guard.test.cjs (17/17 passed)
- [x] {2026-05-12} {feature:plugin} create hooks/agents-md-sync.cjs so CLAUDE.md re-syncs on AGENTS.md edits — done 2026-05-13: substituido por Tier 1 symlink real (`cmd /c mklink CLAUDE.md AGENTS.md`) apos ativar Windows Developer Mode. CLAUDE.md é symbolic link → AGENTS.md, edits propagam instantaneamente, sobrevive a mv/cp/git checkout (testado).
- [x] {2026-05-12} {file:lessons-learned.md.original} migrate BUG-02 (lesson about code blocks in prompts) from `.planning.v5-backup/lessons-learned.md.original` to `docs/compound/` — not covered in fase-05 spec (only 5/6 lessons migrated) — done 2026-05-13: criado docs/compound/2026-05-12-skill-md-code-blocks-do-not-execute.md (category bug-history, tags skills/telemetry/hooks/dogfooding/integration-test); compound:check passa (11 notas) e harness:validate passa (25 required files, 160 md)
- [x] {2026-05-12} {feature:validator} extend harness-validate.ts to detect missing H1 inside skill body (post-frontmatter+comments) without false positives on intentional partial files — done 2026-05-13: nova regra `markdown-heading-skill` usa helper `hasH1OutsideCodeFences` para detectar H1 ignorando fenced code blocks; tolera telemetry block, HTML comments e prose-preface antes do H1 (necessario para skills com profile-aware-preface); normaliza CRLF->LF antes do strip de frontmatter; templates/__fixtures__ ja excluidos via SKIP_DIRS; 161 markdown files passam
- [x] {2026-05-12} {file:.github/workflows/harness.yml} pin actions/checkout and oven-sh/setup-bun to commit SHAs (supply-chain hardening; verify v4 latest SHA from github.com/actions/checkout/releases and v1 SHA from github.com/oven-sh/setup-bun/releases; consider Dependabot pinning:digest) — verify-work plano08 ALTO findings 2,3 — done 2026-05-13: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4 e oven-sh/setup-bun@f4d14e03ff726c06358e5557344e1da148b56cf7 # v1
- [x] {2026-05-12} {file:scripts/harness-validate.ts} add path-traversal boundary check at link checker L235 (`if (!abs.startsWith(root + path.sep) && abs !== root) return`) + sync .tpl — verify-work plano08 ALTO finding 1 — done 2026-05-13: boundary check adicionado pos path.resolve; escapes viram broken-link; .tpl ressincronizado (path-traversal + CRLF normalize + SKILL.md H1 detection + hasH1OutsideCodeFences helper)
- [x] {2026-05-12} {file:scripts/harness-validate.ts} add last-synced-hash guardrail comment + unit test for looksCompleteInline (prevents drift vs orphan-plan-detector.ts canonical) — verify-work plano08 ALTO finding 5 — done 2026-05-13: comentario CANONICAL acima de looksCompleteInline pino o sha256:74b0e7be7053b8f74412c092fbe6e951f99a7b0751ff8c29e26637018dfbf296 das funcoes canonicas (looksComplete + hasRemainingWorkMarker) em skills/init/lib/orphan-plan-detector.ts; novo teste tests/looks-complete-inline.test.ts (2 expects) re-hasheia o canonical em cada run e compara as arrays de regex (signals/markers) entre as duas copias — falha se houver drift
- [x] {2026-05-12} {file:scripts/compound-check.ts} replace parseYamlInline with js-yaml (already in deps) to close bypass surface — verify-work plano08 MEDIO — done 2026-05-13: parseYamlInline/stripQ removidos; yaml.load com CORE_SCHEMA + narrow de unknown→Record antes de ler fields; compound:check passa em 11 notas
- [x] {2026-05-12} {file:AGENTS.md} reformulate L29 from direct agent instruction to "ask the human to run /lessons-learned" — prompt-injection surface — verify-work plano08 MEDIO — done 2026-05-13: "run /lessons-learned" → "ask the human to run /lessons-learned"; CLAUDE.md herda via symlink
- [x] {2026-05-12} {file:scripts/generate-manifest.js} teach generator to emit `skills` top-level index automatically (scan SKILL.md frontmatter for name/version/description) so regeneration does not require manual patch — surfaced by Plano 09 fase-03 regression (tests/todo-pick.test.ts:61-82) — done 2026-05-13: collectSkillsIndex() scaneia skills/<dir>/SKILL.md, parseia frontmatter via js-yaml CORE_SCHEMA, emite `manifest.skills[name] = {path, version, introduced, description}`; 32 skills indexadas (era 1 manual); corrigi YAML invalido em todo-pick e centralize-config (description com `:` ou `{}` agora entre aspas); tests/todo-pick.test.ts e scripts/__tests__/generate-manifest.test.ts passam
- [x] {2026-05-13} {file:hooks/state-md-hook.test.cjs,skills/lib/state-md-generator.test.ts} refatorar testes para usar `os.tmpdir()` em vez de escrever direto em `tests/fixtures/v6-state-fixture/` — elimina GT-01 (working-tree drift apos `bun run test`: `docs/STATE.md` timestamp + `scripts/harness-validate.ts` sync, ver plano09/MEMORY.md L64). Padrao: copiar fixture-template para tmpdir no `beforeEach`, rodar `regenerateStateMd(tmpDir)` contra a copia, `rm -rf` no `afterEach`. Mesma logica para `state-md-hook.test.cjs`. Aceite: rodar `bun run test` 3 vezes consecutivas e `git status` permanecer limpo. — done 2026-08-18: eram **4** arquivos de teste, nao 2. Alem dos dois do titulo (v6-state-fixture), `skills/lib/decision-registry-revoke.test.ts` e `skills/lib/lessons-learned-crud.test.ts` sujavam `adr-revoke-fixture/` e `lessons-crud-fixture/`. Dois padroes distintos: (a) essas duas ultimas ja montavam **toda** a entrada do zero no `beforeEach` — inclusive com `rmSync` na pasta versionada — entao os 5 .md commitados ali eram **saida de teste commitada por acidente**; passaram a montar em `os.tmpdir()` e os dois diretorios sairam do git (`plano06/MEMORY.md` L48 registra que o autor original ja tinha decidido nao versionar `lessons-crud-fixture` e deu unstage nos leftovers — voltou a ser o que era pra ser); (b) `v6-state-fixture` e entrada real (contagens de notas/ADRs/planos/TODO), entao os dois testes de STATE.md copiam a arvore para tmpdir via `fs.cpSync` por teste. Aceite cumprido: 3 `bun run test` consecutivas, `git status` limpo nas 3.
- [x] {2026-05-19} {file:skills/init/lib/run-stack-knowledge-init.ts} v6.3.4+ — `readFileSync(gemfilePath, 'utf8')` em L105 sem size limit (security-auditor MEDIUM, DoS teórico). Refatorar para `fs.promises.stat` + size check ≤1MB antes de ler, e migrar para `fs.promises.readFile` (elimina sync I/O em fluxo async). Ref: plano03/MEMORY.md GT-4 + Findings de Auditores. — done (não datado; descoberto já resolvido na auditoria de 2026-08-18): L117-127 usa `fs.stat` + `GEMFILE_MAX_BYTES` + `fs.readFile` async, com ENOENT como no-op silencioso
- [ ] {2026-05-19} {file:skills/init/lib/atoms-frontmatter-validator.ts} v6.3.4+ — regex `[\s\S]*?` lazy em `extractFrontmatter` (L15) sem closing delimiter guard (security-auditor MEDIUM, ReDoS teórico). Refatorar para regex bounded ou usar `yaml.load` direto. Ref: plano03/MEMORY.md Findings de Auditores. — {2026-08-18} NÃO fechado pelo TODO #6, apesar de tocar a mesma regex (lá só entrou `\r?`). Reavaliação da severidade ao passar pelo arquivo: é quantificador lazy único com âncora literal, sem alternância nem quantificador aninhado — o pior caso é **quadrático** (arquivo grande abrindo `---` e nunca fechando), não exponencial. "ReDoS" superestima; o rótulo honesto é blowup O(n²) em input patológico. Decidir se ainda vale o refactor para `yaml.load` ou se cabe só um cap de tamanho no `readFileSync` (mesmo padrão já aplicado em `run-stack-knowledge-init.ts`).
- [x] {2026-05-19} {file:skills/init/lib/format-knowledge-preview.ts} v6.3.4+ — magic numbers `7`/`1` inline em `extractRailsVersionWarning` (L85) (code-smell MEDIUM). Refatorar para constantes `MIN_SUPPORTED_RAILS_MAJOR=7` / `MIN_SUPPORTED_RAILS_MINOR=1` exportadas. Ref: plano03/MEMORY.md Findings de Auditores. — done 2026-08-18: constantes exportadas conforme pedido, e a **mensagem de warning passou a derivar delas** (`` `cobre ${MAJOR}.${MINOR}+` ``) em vez de repetir "7.1+" em string literal — senao mexer no piso deixaria o texto mentindo, que e o mesmo defeito com outra cara. As constantes ficaram em `format-knowledge-preview.ts` e **nao** no util novo do #5: o piso e sobre a cobertura do knowledge, nao sobre parsing de Gemfile.
- [x] {2026-05-19} {file:skills/init/lib/format-knowledge-preview.ts,skills/init/lib/detect-stack.ts} v6.3.4+ — duplicate regex Rails detection: format-knowledge-preview.ts:78 e detect-stack.ts:98 ambos detectam `gem 'rails'` (code-smell MEDIUM). Extrair `parseRailsAnchor()` util compartilhado. Refinar tambem para Rails anchor exigir `gem 'rails'` (não só presença do Gemfile) — GT-2 do plano03/MEMORY.md. — done 2026-08-18: `parseRailsAnchor()` extraido para `skills/init/lib/rails-anchor.ts`, os dois consumidores passaram a usa-lo, e restou **uma** copia da regex no repo (verificado por grep). Duas observacoes que o item nao previa: (1) as duas regex **nao eram identicas** — `detect-stack` so testava presenca e `format-knowledge-preview` exigia a constraint de versao, entao elas **discordavam** em Gemfile que declara `gem 'rails'` sem versao (git source, versao no lockfile): uma dizia "e Rails", a outra se comportava como se nao houvesse Rails. O util devolve `{present, major, minor}` com versao opcional, o que reconcilia os dois sem mudar o veredito de nenhum caso ja coberto por teste; (2) o refinamento GT-2 **ja estava feito** — `detect-stack` ja exigia `gem 'rails'` e nao a mera presenca do Gemfile. Os tres numeros de linha do item estavam caducos (L78/L98/L85 → L82/L128/L89), o que e `docs/compound/2026-08-17-doc-de-planejamento-e-hipotese-medir-antes-de-editar.md` outra vez. 10 testes novos em `rails-anchor.test.ts`.
- [x] {2026-05-19} {file:scripts/harness-validate.ts} v6.3.4+ — validator regex `/^---\n/` rejeita arquivos com CRLF (BUG-1 da v6.3.3). Aceitar `/^---\r?\n/` para resiliência cross-platform Windows/Linux. Ref: plano03/MEMORY.md GT-1. — done 2026-08-18: o item estava mal escopado. `scripts/harness-validate.ts` já tinha sido corrigido em 2026-05-19 e **nunca esteve quebrado de verdade** (o `normalize` CRLF→LF precede a regex nos dois lados) — a correção lá era defense-in-depth. Os parsers realmente quebrados eram outros três, sem `normalize` nenhum: `skills/lib/exec-plan-reader.ts` (pior caso — `splitFrontmatter` caía no fallback `{frontmatter:''}` e `isComplete()` devolvia false **sem erro**, plano com CRLF parecia incompleto), `skills/init/lib/atoms-frontmatter-validator.ts` ("missing frontmatter block") e `skills/init/lib/compound-writer.ts` ("Frontmatter YAML ausente"). Corrigido nos 3 com `normalize` na entrada + regex `\r?\n` como defense-in-depth. Também alinhado `skills/init/assets/templates/scripts/harness-validate.ts.tpl` (o validador **distribuído** ficou 3 meses atrás do repo — mesma classe de `docs/compound/2026-08-13-suite-verde-nao-exercita-validador-distribuido.md`) e regenerado o checksum no `plugin-manifest.json`. Adicionado `.editorconfig` com `end_of_line = lf` — a prevenção "Imediato" que o próprio compound doc de 2026-05-19 prescreveu e nunca foi aplicada. 7 testes RED→GREEN em 3 arquivos.
- [x] {2026-08-18} {file:skills/init/lib/registry.test.ts} teste `todos os 12 steps sao reais` e **flaky sob a suite completa** (~1 em 6 runs; 5/5 verde isolado). Causa: o teste roda os Steps 9-11 contra `process.cwd()` — o repo vivo — enquanto outros testes escrevem `.claude/stack.json` na mesma raiz (visto o leftover `.claude/stack.json.<pid>.<ts>.tmp` apos uma run). `10-final-validation.ts` L24 le `path.join(cwd, '.claude', 'stack.json')`, entao o veredito do step depende de quem ganhou a corrida. Mesma familia do item de 2026-05-13 acima (teste escrevendo no repo em vez de tmpdir), mas em arquivos diferentes e fora do escopo daquele. Fix provavel: passar um tmpdir como `cwd` no ctx em vez de `process.cwd()`. Descoberto ao validar o aceite do item acima — nao introduzido por ele (o refactor so **reduziu** escrita na raiz). — done 2026-08-18: a hipotese do fix estava certa, mas a causa era mais fechada do que "outros testes escrevem na mesma raiz". O **proprio teste** era o poluidor: o Step 10 (`copy-knowledge`) roda o runner real contra o `cwd` que recebe e grava `.claude/stack.json` la — rodando na raiz, escrevia `primary: nodejs-typescript` no repo de dev. O Step 11 (`final-validation`) le esse mesmo arquivo e **lanca `AbortError`** quando ha stack detectada sem `.claude/knowledge/INDEX.md` (`10-final-validation.ts:50`). Entao o veredito dependia de `.claude/knowledge/` estar intacto naquele instante — poluidor e vitima no mesmo teste. Mecanismo reproduzido de forma deterministica em tmpdir antes de corrigir. **A poluicao era invisivel:** `.claude/stack.json` esta no `.gitignore` L40, entao o side effect nunca apareceu no `git status` — diferente do drift de fixtures, que gritava. Fix: tmpdir compartilhado para os Steps 9-12 com `try/finally`, espelhando a ordem real do pipeline (Step 10 grava `primary: null` no tmpdir vazio, Step 11 le e nao aborta). Removido tambem o `as any` do ctx — o tipo real aceitava a forma. Verificacao: 10 suites completas consecutivas, 0 falhas, e `.claude/stack.json` **nao reapareceu em nenhuma** — o que prova que este teste era o unico escritor daquele caminho na raiz. Cobertura do caminho de abort ja existia e estava correta em `10-final-validation.test.ts` (em tmpdir) — nao foi preciso teste novo.
